import sys

EXPECTED_RUNTIME_ERRORS = (TypeError, ValueError, RuntimeError, AttributeError, IndexError, KeyError, OSError)


def _viewer_runtime_module(viewer):
    try:
        return sys.modules.get(viewer.__class__.__module__)
    except EXPECTED_RUNTIME_ERRORS:
        return None


def _bind_impl(viewer, export_name: str):
    module = _viewer_runtime_module(viewer)
    if module is None:
        raise RuntimeError("viewer runtime module unavailable")
    impl_name = f"_drop_viewer_unknown_mods_impl_{export_name}"
    if hasattr(module, impl_name):
        return getattr(module, impl_name)
    source = _SOURCES[export_name]
    exec(source, module.__dict__, module.__dict__)
    return getattr(module, impl_name)


_SOURCES = {
    'load_mod_database': 'def _drop_viewer_unknown_mods_impl_load_mod_database(viewer):\n    if ModDatabase is None:\n        return None\n    candidate_dirs = []\n    try:\n        sources_root = os.path.dirname(os.path.dirname(os.path.dirname(__file__)))\n        candidate_dirs.append(os.path.join(sources_root, "marks_sources", "mods_data"))\n    except EXPECTED_RUNTIME_ERRORS:\n        pass\n    try:\n        project_root = PathLocator.get_project_root_directory()\n        candidate_dirs.append(os.path.join(project_root, "Sources", "marks_sources", "mods_data"))\n    except EXPECTED_RUNTIME_ERRORS:\n        pass\n\n    seen = set()\n    for data_dir in candidate_dirs:\n        norm = os.path.normcase(os.path.normpath(viewer._ensure_text(data_dir)))\n        if not norm or norm in seen:\n            continue\n        seen.add(norm)\n        if not os.path.isdir(data_dir):\n            continue\n        try:\n            return ModDatabase.load(data_dir)\n        except EXPECTED_RUNTIME_ERRORS:\n            continue\n    return None',
    'collect_known_mod_ids': 'def _drop_viewer_unknown_mods_impl_collect_known_mod_ids(viewer) -> set[int]:\n    known_ids: set[int] = set()\n    db = viewer.mod_db\n    if db is None:\n        return known_ids\n    try:\n        for weapon_mod in list(getattr(db, "weapon_mods", {}).values()):\n            for mod in list(getattr(weapon_mod, "modifiers", []) or []):\n                ident = max(0, int(getattr(mod, "identifier", 0)))\n                if ident > 0:\n                    known_ids.add(ident)\n    except EXPECTED_RUNTIME_ERRORS:\n        pass\n    try:\n        for rune in list(getattr(db, "runes", {}).values()):\n            for mod in list(getattr(rune, "modifiers", []) or []):\n                ident = max(0, int(getattr(mod, "identifier", 0)))\n                if ident > 0:\n                    known_ids.add(ident)\n    except EXPECTED_RUNTIME_ERRORS:\n        pass\n    return known_ids',
    'normalize_unknown_mod_entry': 'def _drop_viewer_unknown_mods_impl_normalize_unknown_mod_entry(viewer, entry: Any) -> dict[str, Any]:\n    normalized = {\n        "count": 0,\n        "arg1_min": None,\n        "arg1_max": None,\n        "arg2_min": None,\n        "arg2_max": None,\n        "item_types": [],\n        "model_ids": [],\n        "sources": {},\n        "sample_items": [],\n        "sample_events": [],\n        "co_mod_ids": [],\n        "last_seen": "",\n    }\n    if not isinstance(entry, dict):\n        return normalized\n    try:\n        normalized["count"] = max(0, int(entry.get("count", 0)))\n    except EXPECTED_RUNTIME_ERRORS:\n        normalized["count"] = 0\n\n    for key in ("arg1_min", "arg1_max", "arg2_min", "arg2_max"):\n        try:\n            value = entry.get(key, None)\n            normalized[key] = None if value is None else int(value)\n        except EXPECTED_RUNTIME_ERRORS:\n            normalized[key] = None\n\n    for key, cap in (("item_types", 24), ("model_ids", 40), ("sample_items", 12), ("sample_events", 12), ("co_mod_ids", 40)):\n        values = []\n        try:\n            raw = list(entry.get(key, []) or [])\n        except EXPECTED_RUNTIME_ERRORS:\n            raw = []\n        for raw_value in raw:\n            if key in ("sample_items", "sample_events"):\n                val = viewer._ensure_text(raw_value).strip()\n                if not val or val in values:\n                    continue\n                values.append(val)\n            else:\n                try:\n                    val = int(raw_value)\n                except EXPECTED_RUNTIME_ERRORS:\n                    continue\n                if val <= 0 or val in values:\n                    continue\n                values.append(val)\n            if len(values) >= cap:\n                break\n        normalized[key] = values\n\n    source_map = {}\n    try:\n        raw_sources = entry.get("sources", {}) or {}\n        if isinstance(raw_sources, dict):\n            for src, count in raw_sources.items():\n                src_key = viewer._ensure_text(src).strip() or "unknown"\n                try:\n                    src_count = max(0, int(count))\n                except EXPECTED_RUNTIME_ERRORS:\n                    src_count = 0\n                if src_count > 0:\n                    source_map[src_key] = src_count\n    except EXPECTED_RUNTIME_ERRORS:\n        source_map = {}\n    normalized["sources"] = source_map\n    normalized["last_seen"] = viewer._ensure_text(entry.get("last_seen", "")).strip()\n    return normalized',
    'load_unknown_mod_catalog': 'def _drop_viewer_unknown_mods_impl_load_unknown_mod_catalog(viewer):\n    viewer.unknown_mod_catalog = {}\n    path = viewer._ensure_text(viewer.unknown_mod_catalog_path).strip()\n    if not path or not os.path.exists(path):\n        return\n    try:\n        with open(path, mode="r", encoding="utf-8") as f:\n            loaded = json.load(f)\n        if isinstance(loaded, dict) and isinstance(loaded.get("unknown_mods", None), dict):\n            loaded = loaded.get("unknown_mods", {})\n        if not isinstance(loaded, dict):\n            return\n        normalized_catalog = {}\n        for key, entry in loaded.items():\n            try:\n                ident = max(0, int(key))\n            except EXPECTED_RUNTIME_ERRORS:\n                continue\n            if ident <= 0:\n                continue\n            normalized_catalog[str(ident)] = viewer._normalize_unknown_mod_entry(entry)\n        viewer.unknown_mod_catalog = normalized_catalog\n    except EXPECTED_RUNTIME_ERRORS:\n        viewer.unknown_mod_catalog = {}',
    'load_unknown_mod_name_map': 'def _drop_viewer_unknown_mods_impl_load_unknown_mod_name_map(viewer):\n    viewer.unknown_mod_custom_names = {}\n    path = viewer._ensure_text(viewer.unknown_mod_name_map_path).strip()\n    if not path or not os.path.exists(path):\n        return\n    try:\n        with open(path, mode="r", encoding="utf-8") as f:\n            loaded = json.load(f)\n        if isinstance(loaded, dict) and isinstance(loaded.get("names", None), dict):\n            loaded = loaded.get("names", {})\n        if not isinstance(loaded, dict):\n            return\n        mapped = {}\n        for raw_key, raw_name in loaded.items():\n            ident = max(0, viewer._safe_int(raw_key, 0))\n            name_txt = viewer._ensure_text(raw_name).strip()\n            if ident <= 0 or not name_txt:\n                continue\n            mapped[str(ident)] = name_txt[:120]\n        viewer.unknown_mod_custom_names = mapped\n    except EXPECTED_RUNTIME_ERRORS:\n        viewer.unknown_mod_custom_names = {}',
    'load_unknown_mod_pending_notes': 'def _drop_viewer_unknown_mods_impl_load_unknown_mod_pending_notes(viewer):\n    viewer.unknown_mod_pending_notes = {}\n    path = viewer._ensure_text(viewer.unknown_mod_pending_notes_path).strip()\n    if not path or not os.path.exists(path):\n        return\n    try:\n        with open(path, mode="r", encoding="utf-8") as f:\n            loaded = json.load(f)\n        if isinstance(loaded, dict) and isinstance(loaded.get("pending", None), dict):\n            loaded = loaded.get("pending", {})\n        if not isinstance(loaded, dict):\n            return\n        normalized = {}\n        for raw_key, raw_value in loaded.items():\n            ident = max(0, viewer._safe_int(raw_key, 0))\n            if ident <= 0:\n                continue\n            if not isinstance(raw_value, dict):\n                continue\n            note = {\n                "id": ident,\n                "owner": viewer._ensure_text(raw_value.get("owner", "")).strip(),\n                "item": viewer._clean_item_name(raw_value.get("item", "")),\n                "arg1": viewer._safe_int(raw_value.get("arg1", 0), 0),\n                "arg2": viewer._safe_int(raw_value.get("arg2", 0), 0),\n                "item_type": viewer._safe_int(raw_value.get("item_type", 0), 0),\n                "model_id": viewer._safe_int(raw_value.get("model_id", 0), 0),\n                "source": viewer._ensure_text(raw_value.get("source", "")).strip(),\n                "first_seen": viewer._ensure_text(raw_value.get("first_seen", "")).strip(),\n                "last_seen": viewer._ensure_text(raw_value.get("last_seen", "")).strip(),\n                "count": max(1, viewer._safe_int(raw_value.get("count", 1), 1)),\n            }\n            normalized[str(ident)] = note\n        viewer.unknown_mod_pending_notes = normalized\n    except EXPECTED_RUNTIME_ERRORS:\n        viewer.unknown_mod_pending_notes = {}',
    'save_unknown_mod_pending_notes': 'def _drop_viewer_unknown_mods_impl_save_unknown_mod_pending_notes(viewer) -> bool:\n    path = viewer._ensure_text(viewer.unknown_mod_pending_notes_path).strip()\n    if not path:\n        return False\n    try:\n        os.makedirs(os.path.dirname(path), exist_ok=True)\n        payload_notes = {}\n        for key in sorted(viewer.unknown_mod_pending_notes.keys(), key=lambda value: int(value)):\n            ident = max(0, viewer._safe_int(key, 0))\n            if ident <= 0:\n                continue\n            note = dict(viewer.unknown_mod_pending_notes.get(key, {}) or {})\n            if not note:\n                continue\n            payload_notes[str(ident)] = {\n                "id": ident,\n                "owner": viewer._ensure_text(note.get("owner", "")).strip(),\n                "item": viewer._clean_item_name(note.get("item", "")),\n                "arg1": viewer._safe_int(note.get("arg1", 0), 0),\n                "arg2": viewer._safe_int(note.get("arg2", 0), 0),\n                "item_type": viewer._safe_int(note.get("item_type", 0), 0),\n                "model_id": viewer._safe_int(note.get("model_id", 0), 0),\n                "source": viewer._ensure_text(note.get("source", "")).strip(),\n                "first_seen": viewer._ensure_text(note.get("first_seen", "")).strip(),\n                "last_seen": viewer._ensure_text(note.get("last_seen", "")).strip(),\n                "count": max(1, viewer._safe_int(note.get("count", 1), 1)),\n            }\n        payload = {\n            "updated_at": datetime.datetime.now().strftime("%Y-%m-%d %H:%M:%S"),\n            "pending": payload_notes,\n        }\n        with open(path, mode="w", encoding="utf-8") as f:\n            json.dump(payload, f, indent=2)\n        return True\n    except EXPECTED_RUNTIME_ERRORS as e:\n        Py4GW.Console.Log("DropViewer", f"Unknown mod pending save failed: {e}", Py4GW.Console.MessageType.Warning)\n        return False',
    'upsert_unknown_mod_pending_note': 'def _drop_viewer_unknown_mods_impl_upsert_unknown_mod_pending_note(\n    viewer,\n    ident: int,\n    owner_name: str = "",\n    item_name: str = "",\n    arg1: int = 0,\n    arg2: int = 0,\n    item_type: int = 0,\n    model_id: int = 0,\n    source: str = "",\n):\n    ident_i = max(0, viewer._safe_int(ident, 0))\n    if ident_i <= 0:\n        return\n    key = str(ident_i)\n    now_txt = datetime.datetime.now().strftime("%Y-%m-%d %H:%M:%S")\n    note = dict(viewer.unknown_mod_pending_notes.get(key, {}) or {})\n    if not note:\n        note = {\n            "id": ident_i,\n            "owner": viewer._ensure_text(owner_name).strip(),\n            "item": viewer._clean_item_name(item_name),\n            "arg1": viewer._safe_int(arg1, 0),\n            "arg2": viewer._safe_int(arg2, 0),\n            "item_type": viewer._safe_int(item_type, 0),\n            "model_id": viewer._safe_int(model_id, 0),\n            "source": viewer._ensure_text(source).strip(),\n            "first_seen": now_txt,\n            "last_seen": now_txt,\n            "count": 1,\n        }\n    else:\n        note["owner"] = viewer._ensure_text(owner_name).strip() or viewer._ensure_text(note.get("owner", "")).strip()\n        note["item"] = viewer._clean_item_name(item_name) or viewer._clean_item_name(note.get("item", ""))\n        note["arg1"] = viewer._safe_int(arg1, note.get("arg1", 0))\n        note["arg2"] = viewer._safe_int(arg2, note.get("arg2", 0))\n        note["item_type"] = viewer._safe_int(item_type, note.get("item_type", 0))\n        note["model_id"] = viewer._safe_int(model_id, note.get("model_id", 0))\n        note["source"] = viewer._ensure_text(source).strip() or viewer._ensure_text(note.get("source", "")).strip()\n        note["last_seen"] = now_txt\n        note["count"] = max(1, viewer._safe_int(note.get("count", 1), 1) + 1)\n        if not viewer._ensure_text(note.get("first_seen", "")).strip():\n            note["first_seen"] = now_txt\n    viewer.unknown_mod_pending_notes[key] = note\n    viewer._save_unknown_mod_pending_notes()',
    'remove_unknown_mod_pending_note': 'def _drop_viewer_unknown_mods_impl_remove_unknown_mod_pending_note(viewer, ident: int) -> bool:\n    ident_i = max(0, viewer._safe_int(ident, 0))\n    if ident_i <= 0:\n        return False\n    key = str(ident_i)\n    if key not in viewer.unknown_mod_pending_notes:\n        return False\n    viewer.unknown_mod_pending_notes.pop(key, None)\n    return viewer._save_unknown_mod_pending_notes()',
    'unknown_mod_pending_lines': 'def _drop_viewer_unknown_mods_impl_unknown_mod_pending_lines(viewer, limit: int = 8) -> list[str]:\n    rows = []\n    for key, note in viewer.unknown_mod_pending_notes.items():\n        ident = max(0, viewer._safe_int(key, 0))\n        if ident <= 0:\n            continue\n        count = max(1, viewer._safe_int(note.get("count", 1), 1))\n        owner = viewer._ensure_text(note.get("owner", "")).strip() or "Unknown"\n        item = viewer._clean_item_name(note.get("item", "")) or "Unknown Item"\n        arg1 = viewer._safe_int(note.get("arg1", 0), 0)\n        arg2 = viewer._safe_int(note.get("arg2", 0), 0)\n        rows.append((count, ident, owner, item, arg1, arg2))\n    rows.sort(key=lambda row: (-row[0], row[1]))\n    return [\n        f"id={ident} x{count} | {owner} | {item} | a1={arg1} a2={arg2}"\n        for count, ident, owner, item, arg1, arg2 in rows[: max(1, int(limit))]\n    ]',
    'unknown_mod_pending_count': 'def _drop_viewer_unknown_mods_impl_unknown_mod_pending_count(viewer) -> int:\n    return len(viewer.unknown_mod_pending_notes)',
    'save_unknown_mod_name_map': 'def _drop_viewer_unknown_mods_impl_save_unknown_mod_name_map(viewer) -> bool:\n    path = viewer._ensure_text(viewer.unknown_mod_name_map_path).strip()\n    if not path:\n        return False\n    try:\n        os.makedirs(os.path.dirname(path), exist_ok=True)\n        sorted_names = {}\n        for key in sorted(viewer.unknown_mod_custom_names.keys(), key=lambda value: int(value)):\n            name_txt = viewer._ensure_text(viewer.unknown_mod_custom_names.get(key, "")).strip()\n            if name_txt:\n                sorted_names[key] = name_txt\n        payload = {\n            "updated_at": datetime.datetime.now().strftime("%Y-%m-%d %H:%M:%S"),\n            "names": sorted_names,\n        }\n        with open(path, mode="w", encoding="utf-8") as f:\n            json.dump(payload, f, indent=2)\n        return True\n    except EXPECTED_RUNTIME_ERRORS as e:\n        Py4GW.Console.Log("DropViewer", f"Unknown mod name map save failed: {e}", Py4GW.Console.MessageType.Warning)\n        return False',
    'set_unknown_mod_custom_name': 'def _drop_viewer_unknown_mods_impl_set_unknown_mod_custom_name(viewer, ident: int, name: str) -> bool:\n    ident_i = max(0, viewer._safe_int(ident, 0))\n    if ident_i <= 0:\n        return False\n    key = str(ident_i)\n    name_txt = viewer._ensure_text(name).strip()\n    if name_txt:\n        viewer.unknown_mod_custom_names[key] = name_txt[:120]\n        viewer.unknown_mod_name_edit_id = ident_i\n        viewer.unknown_mod_name_edit_text = viewer.unknown_mod_custom_names[key]\n        viewer._push_unknown_mod_notification(f"Mapped unknown mod id={ident_i} -> {viewer.unknown_mod_custom_names[key]}")\n        viewer._remove_unknown_mod_pending_note(ident_i)\n        if viewer._save_unknown_mod_name_map():\n            return True\n        return False\n    if key in viewer.unknown_mod_custom_names:\n        viewer.unknown_mod_custom_names.pop(key, None)\n        viewer.unknown_mod_name_edit_id = ident_i\n        viewer.unknown_mod_name_edit_text = ""\n        if viewer._save_unknown_mod_name_map():\n            return True\n        return False\n    return False',
    'get_unknown_mod_custom_name': 'def _drop_viewer_unknown_mods_impl_get_unknown_mod_custom_name(viewer, ident: int) -> str:\n    ident_i = max(0, viewer._safe_int(ident, 0))\n    if ident_i <= 0:\n        return ""\n    return viewer._ensure_text(viewer.unknown_mod_custom_names.get(str(ident_i), "")).strip()',
    'unknown_mod_name_map_summary_lines': 'def _drop_viewer_unknown_mods_impl_unknown_mod_name_map_summary_lines(viewer, limit: int = 4) -> list[str]:\n    rows = []\n    for key, val in viewer.unknown_mod_custom_names.items():\n        ident = max(0, viewer._safe_int(key, 0))\n        name_txt = viewer._ensure_text(val).strip()\n        if ident > 0 and name_txt:\n            rows.append((ident, name_txt))\n    rows.sort(key=lambda it: it[0])\n    return [f"mapped id={ident}: {name_txt}" for ident, name_txt in rows[: max(1, int(limit))]]',
    'collect_manual_named_mod_lines': 'def _drop_viewer_unknown_mods_impl_collect_manual_named_mod_lines(viewer, raw_mods) -> list[str]:\n    lines = []\n    if not raw_mods:\n        return lines\n    seen = set()\n    for mod in list(raw_mods or []):\n        if not isinstance(mod, (list, tuple)) or len(mod) < 3:\n            continue\n        ident = max(0, viewer._safe_int(mod[0], 0))\n        if ident <= 0:\n            continue\n        custom_name = viewer._get_unknown_mod_custom_name(ident)\n        if not custom_name:\n            continue\n        arg1 = viewer._safe_int(mod[1], 0)\n        arg2 = viewer._safe_int(mod[2], 0)\n        line = custom_name if (arg1 == 0 and arg2 == 0) else f"{custom_name} [{arg1},{arg2}]"\n        key = re.sub(r"[^a-z0-9]+", "", f"{ident}:{line}".lower())\n        if key in seen:\n            continue\n        seen.add(key)\n        lines.append(line)\n    return lines',
    'save_unknown_mod_catalog': 'def _drop_viewer_unknown_mods_impl_save_unknown_mod_catalog(viewer):\n    path = viewer._ensure_text(viewer.unknown_mod_catalog_path).strip()\n    if not path:\n        return False\n    try:\n        os.makedirs(os.path.dirname(path), exist_ok=True)\n        sorted_entries = {}\n        for key in sorted(viewer.unknown_mod_catalog.keys(), key=lambda value: int(value)):\n            sorted_entries[key] = viewer._normalize_unknown_mod_entry(viewer.unknown_mod_catalog.get(key, {}))\n        payload = {\n            "updated_at": datetime.datetime.now().strftime("%Y-%m-%d %H:%M:%S"),\n            "known_mod_id_count": int(len(viewer.known_mod_ids)),\n            "unknown_mods": sorted_entries,\n        }\n        with open(path, mode="w", encoding="utf-8") as f:\n            json.dump(payload, f, indent=2)\n        return True\n    except EXPECTED_RUNTIME_ERRORS as e:\n        Py4GW.Console.Log("DropViewer", f"Unknown mod catalog save failed: {e}", Py4GW.Console.MessageType.Warning)\n        return False',
    'flush_unknown_mod_catalog_if_dirty': 'def _drop_viewer_unknown_mods_impl_flush_unknown_mod_catalog_if_dirty(viewer, force: bool = False):\n    if not viewer.unknown_mod_catalog_dirty:\n        return\n    if not force and not viewer.unknown_mod_catalog_flush_timer.IsExpired():\n        return\n    viewer.unknown_mod_catalog_flush_timer.Reset()\n    if viewer._save_unknown_mod_catalog():\n        viewer.unknown_mod_catalog_dirty = False',
    'get_unknown_mod_count': 'def _drop_viewer_unknown_mods_impl_get_unknown_mod_count(viewer) -> int:\n    return len(viewer.unknown_mod_catalog)',
    'is_known_or_excluded_mod_id': 'def _drop_viewer_unknown_mods_impl_is_known_or_excluded_mod_id(viewer, ident: int) -> bool:\n    ident_i = max(0, viewer._safe_int(ident, 0))\n    if ident_i <= 0:\n        return True\n    if ident_i in viewer.known_mod_ids:\n        return True\n    if viewer._get_unknown_mod_custom_name(ident_i):\n        return True\n    if ident_i in viewer.unknown_mod_exclude_ids:\n        return True\n    if ident_i in UNKNOWN_MOD_KNOWN_NAME_HINTS:\n        return True\n    return False',
    'unknown_mod_item_type_labels': 'def _drop_viewer_unknown_mods_impl_unknown_mod_item_type_labels(viewer, item_types, limit: int = 3) -> list[str]:\n    labels = []\n    for raw_type in list(item_types or []):\n        item_type_int = max(0, viewer._safe_int(raw_type, 0))\n        if item_type_int <= 0:\n            continue\n        type_name = UNKNOWN_MOD_ITEM_TYPE_HINTS.get(item_type_int, "")\n        if not type_name and ItemType is not None:\n            try:\n                type_name = viewer._ensure_text(getattr(ItemType(item_type_int), "name", "")).strip()\n            except EXPECTED_RUNTIME_ERRORS:\n                type_name = ""\n        label = f"{item_type_int}({type_name})" if type_name else str(item_type_int)\n        if label not in labels:\n            labels.append(label)\n        if len(labels) >= max(1, int(limit)):\n            break\n    return labels',
    'unknown_mod_summary_lines': 'def _drop_viewer_unknown_mods_impl_unknown_mod_summary_lines(viewer, limit: int = 8) -> list[str]:\n    lines = []\n    if not viewer.unknown_mod_catalog:\n        return lines\n    entries = []\n    for key, value in viewer.unknown_mod_catalog.items():\n        try:\n            ident = int(key)\n            count = int((value or {}).get("count", 0))\n        except EXPECTED_RUNTIME_ERRORS:\n            continue\n        entries.append((count, ident, value or {}))\n    entries.sort(key=lambda item: (-item[0], item[1]))\n    for count, ident, value in entries[: max(1, int(limit))]:\n        arg1_min = value.get("arg1_min", None)\n        arg1_max = value.get("arg1_max", None)\n        arg2_min = value.get("arg2_min", None)\n        arg2_max = value.get("arg2_max", None)\n        item_types = ",".join(viewer._unknown_mod_item_type_labels(value.get("item_types", []), limit=3))\n        lines.append(\n            f"id={ident} hits={count} "\n            f"a1=[{arg1_min},{arg1_max}] "\n            f"a2=[{arg2_min},{arg2_max}] "\n            f"types=[{item_types}]"\n        )\n    return lines',
    'push_unknown_mod_notification': 'def _drop_viewer_unknown_mods_impl_push_unknown_mod_notification(viewer, line: str):\n    msg = viewer._ensure_text(line).strip()\n    if not msg:\n        return\n    max_items = max(3, int(getattr(viewer, "unknown_mod_recent_notifications_max", 10)))\n    notes = list(getattr(viewer, "unknown_mod_recent_notifications", []) or [])\n    if not notes or notes[0] != msg:\n        notes.insert(0, msg)\n    viewer.unknown_mod_recent_notifications = notes[:max_items]',
    'unknown_mod_notification_lines': 'def _drop_viewer_unknown_mods_impl_unknown_mod_notification_lines(viewer, limit: int = 4) -> list[str]:\n    notes = list(getattr(viewer, "unknown_mod_recent_notifications", []) or [])\n    out = []\n    for line in notes[: max(1, int(limit))]:\n        txt = viewer._ensure_text(line).strip()\n        if txt:\n            out.append(txt)\n    return out',
    'notify_new_unknown_mod_discovered': 'def _drop_viewer_unknown_mods_impl_notify_new_unknown_mod_discovered(\n    viewer,\n    ident: int,\n    arg1: int,\n    arg2: int,\n    owner_name: str = "",\n    item_name: str = "",\n    item_type: int = 0,\n    model_id: int = 0,\n    source: str = "",\n):\n    if not bool(getattr(viewer, "unknown_mod_notify_enabled", True)):\n        return\n    ident_i = max(0, viewer._safe_int(ident, 0))\n    if ident_i <= 0:\n        return\n    owner_txt = viewer._ensure_text(owner_name).strip() or "Unknown"\n    item_txt = viewer._clean_item_name(item_name) or "Unknown Item"\n    source_txt = viewer._ensure_text(source).strip() or "unknown"\n    item_kind_labels = viewer._unknown_mod_item_type_labels([item_type], limit=1)\n    item_kind_txt = item_kind_labels[0] if item_kind_labels else str(int(item_type))\n    msg = (\n        f"New unknown mod: id={ident_i} owner=\'{owner_txt}\' item=\'{item_txt}\' "\n        f"a1={int(arg1)} a2={int(arg2)} type={item_kind_txt} model={int(model_id)} src={source_txt}"\n    )\n    viewer._upsert_unknown_mod_pending_note(\n        ident=ident_i,\n        owner_name=owner_txt,\n        item_name=item_txt,\n        arg1=int(arg1),\n        arg2=int(arg2),\n        item_type=int(item_type),\n        model_id=int(model_id),\n        source=source_txt,\n    )\n    viewer._push_unknown_mod_notification(msg)\n    Py4GW.Console.Log("DropViewer", msg, Py4GW.Console.MessageType.Info)\n    if bool(getattr(viewer, "unknown_mod_popup_enabled", True)):\n        viewer.unknown_mod_popup_message = msg\n        viewer.unknown_mod_popup_pending = True\n    now_ts = time.time()\n    last_status_ts = float(getattr(viewer, "unknown_mod_last_notify_status_ts", 0.0) or 0.0)\n    if (now_ts - last_status_ts) >= 0.7:\n        viewer.unknown_mod_last_notify_status_ts = now_ts\n        viewer.set_status(msg)',
    'guess_unknown_mod_entry': 'def _drop_viewer_unknown_mods_impl_guess_unknown_mod_entry(viewer, ident: int, entry: dict[str, Any]) -> dict[str, Any]:\n    ident_i = max(0, viewer._safe_int(ident, 0))\n    normalized = viewer._normalize_unknown_mod_entry(entry)\n    count = max(0, viewer._safe_int(normalized.get("count", 0), 0))\n    arg1_min = normalized.get("arg1_min", None)\n    arg1_max = normalized.get("arg1_max", None)\n    arg2_min = normalized.get("arg2_min", None)\n    arg2_max = normalized.get("arg2_max", None)\n    item_types = [max(0, viewer._safe_int(v, 0)) for v in list(normalized.get("item_types", []) or [])]\n    item_types = [v for v in item_types if v > 0]\n    co_mod_ids = [max(0, viewer._safe_int(v, 0)) for v in list(normalized.get("co_mod_ids", []) or [])]\n    co_mod_ids = [v for v in co_mod_ids if v > 0 and v != ident_i]\n    sample_items = [viewer._clean_item_name(v) for v in list(normalized.get("sample_items", []) or [])]\n    sample_items = [v for v in sample_items if v]\n    sample_items_lower = [v.lower() for v in sample_items]\n    co_mod_set = set(co_mod_ids)\n    item_type_set = set(item_types)\n\n    custom_name = viewer._get_unknown_mod_custom_name(ident_i)\n    if custom_name:\n        return {\n            "guess": custom_name,\n            "confidence": 100,\n            "confidence_label": "High",\n            "basis": ["manual mapping"],\n            "is_known_hint": True,\n        }\n\n    known_hint = viewer._ensure_text(UNKNOWN_MOD_KNOWN_NAME_HINTS.get(ident_i, "")).strip()\n    if known_hint:\n        return {\n            "guess": known_hint,\n            "confidence": 100,\n            "confidence_label": "High",\n            "basis": ["known modifier hint"],\n            "is_known_hint": True,\n        }\n\n    guess = "Unmapped modifier (manual label needed)"\n    score = 28\n    basis = []\n\n    caster_markers = {8728, 8920, 8952, 8968, 8984, 9000, 9112, 9128, 9880, 25288, 26568}\n    base_stat_markers = {10136, 42120, 42920, 42936}\n    armor_markers = {8408, 8680, 9224, 9520, 10218, 41928, 42288}\n\n    if sample_items and all("tome" in name for name in sample_items_lower[: min(3, len(sample_items_lower))]):\n        guess = "Tome metadata/profession marker"\n        score = 66\n        basis.append("seen only on Tome items")\n    else:\n        material_tokens = ("ingot", "hide", "square", "dust", "wood", "cloth", "fiber", "bone", "scale")\n        is_material_name = any(any(token in name for token in material_tokens) for name in sample_items_lower)\n        if is_material_name and co_mod_set.issubset({49152}):\n            guess = "Material stack marker"\n            score = 62\n            basis.append("material names + structural co-mods")\n\n    if guess.startswith("Unmapped"):\n        if item_type_set & {12, 22, 26} and co_mod_set & caster_markers:\n            guess = "Caster weapon mod family (HCT/HSR/Energy)"\n            score = 64\n            basis.append("caster item types + caster co-mod IDs")\n        elif item_type_set & {24, 27} and co_mod_set & base_stat_markers:\n            guess = "Melee/shield companion modifier"\n            score = 55\n            basis.append("melee/shield co-mod IDs")\n\n    if co_mod_set & armor_markers:\n        if arg1_min == arg1_max == 40 and arg2_min == arg2_max == 0:\n            guess = "Armor bonus scalar (+40 family)"\n            score = max(score, 74)\n            basis.append("fixed a1=40 with armor markers")\n        elif arg1_min == arg1_max == 0 and arg2_min == arg2_max == 10:\n            guess = "Armor bonus scalar (+10 family)"\n            score = max(score, 68)\n            basis.append("fixed a2=10 with armor markers")\n        elif guess.startswith("Unmapped"):\n            guess = "Armor/rune metadata modifier"\n            score = max(score, 58)\n            basis.append("co-occurs with armor/rune markers")\n\n    if arg1_min == arg1_max == 0 and arg2_min == arg2_max == 0 and len(co_mod_set) >= 3:\n        if guess.startswith("Unmapped"):\n            guess = "Structural marker (no direct tooltip line)"\n            score = max(score, 52)\n            basis.append("always zero args + broad co-occurrence")\n\n    if count >= 25:\n        score += 12\n        basis.append("high observation count")\n    elif count >= 10:\n        score += 8\n    elif count >= 5:\n        score += 4\n    elif count <= 2:\n        score -= 8\n\n    score = max(15, min(99, int(score)))\n    if score >= 85:\n        confidence_label = "High"\n    elif score >= 60:\n        confidence_label = "Medium"\n    else:\n        confidence_label = "Low"\n\n    if not basis:\n        basis.append("not enough correlated data yet")\n\n    return {\n        "guess": guess,\n        "confidence": score,\n        "confidence_label": confidence_label,\n        "basis": list(dict.fromkeys(basis))[:4],\n        "is_known_hint": False,\n    }',
    'unknown_mod_guess_report_entries': 'def _drop_viewer_unknown_mods_impl_unknown_mod_guess_report_entries(viewer, include_known: bool = True) -> list[dict[str, Any]]:\n    entries = []\n    for key, value in viewer.unknown_mod_catalog.items():\n        ident = max(0, viewer._safe_int(key, 0))\n        if ident <= 0:\n            continue\n        normalized = viewer._normalize_unknown_mod_entry(value or {})\n        guess = viewer._guess_unknown_mod_entry(ident, normalized)\n        if not include_known and bool(guess.get("is_known_hint", False)):\n            continue\n        item_types = [max(0, viewer._safe_int(v, 0)) for v in list(normalized.get("item_types", []) or [])]\n        item_types = [v for v in item_types if v > 0]\n        co_mod_ids = [max(0, viewer._safe_int(v, 0)) for v in list(normalized.get("co_mod_ids", []) or [])]\n        co_mod_ids = [v for v in co_mod_ids if v > 0 and v != ident]\n        sample_items = [viewer._clean_item_name(v) for v in list(normalized.get("sample_items", []) or [])]\n        sample_items = [v for v in sample_items if v]\n        entries.append(\n            {\n                "id": ident,\n                "hits": max(0, viewer._safe_int(normalized.get("count", 0), 0)),\n                "guess": viewer._ensure_text(guess.get("guess", "")).strip() or "Unmapped modifier",\n                "confidence": max(0, min(100, viewer._safe_int(guess.get("confidence", 0), 0))),\n                "confidence_label": viewer._ensure_text(guess.get("confidence_label", "")).strip() or "Low",\n                "is_known_hint": bool(guess.get("is_known_hint", False)),\n                "basis": list(guess.get("basis", []) or []),\n                "arg1_min": normalized.get("arg1_min", None),\n                "arg1_max": normalized.get("arg1_max", None),\n                "arg2_min": normalized.get("arg2_min", None),\n                "arg2_max": normalized.get("arg2_max", None),\n                "item_types": item_types,\n                "item_type_labels": viewer._unknown_mod_item_type_labels(item_types, limit=12),\n                "sample_items": sample_items[:12],\n                "co_mod_ids": co_mod_ids[:40],\n                "last_seen": viewer._ensure_text(normalized.get("last_seen", "")).strip(),\n            }\n        )\n    entries.sort(key=lambda row: (-int(row.get("hits", 0)), int(row.get("id", 0))))\n    return entries',
    'get_unknown_mod_unresolved_count': 'def _drop_viewer_unknown_mods_impl_get_unknown_mod_unresolved_count(viewer) -> int:\n    return len(viewer._unknown_mod_guess_report_entries(include_known=False))',
    'unknown_mod_guess_summary_lines': 'def _drop_viewer_unknown_mods_impl_unknown_mod_guess_summary_lines(viewer, limit: int = 8, include_known: bool = False) -> list[str]:\n    lines = []\n    entries = viewer._unknown_mod_guess_report_entries(include_known=include_known)\n    for row in entries[: max(1, int(limit))]:\n        ident = max(0, viewer._safe_int(row.get("id", 0), 0))\n        hits = max(0, viewer._safe_int(row.get("hits", 0), 0))\n        confidence = max(0, min(100, viewer._safe_int(row.get("confidence", 0), 0)))\n        confidence_label = viewer._ensure_text(row.get("confidence_label", "")).strip() or "Low"\n        guess = viewer._ensure_text(row.get("guess", "")).strip() or "Unmapped modifier"\n        basis = list(row.get("basis", []) or [])\n        basis_txt = f" ({basis[0]})" if basis else ""\n        lines.append(\n            f"id={ident} hits={hits} {confidence_label}({confidence}%): {guess}{basis_txt}"\n        )\n    return lines',
    'export_unknown_mod_guess_report': 'def _drop_viewer_unknown_mods_impl_export_unknown_mod_guess_report(viewer, include_known: bool = True) -> str:\n    viewer._flush_unknown_mod_catalog_if_dirty(force=True)\n    path = viewer._ensure_text(viewer.unknown_mod_guess_report_path).strip()\n    if not path:\n        return ""\n    try:\n        os.makedirs(os.path.dirname(path), exist_ok=True)\n        entries = viewer._unknown_mod_guess_report_entries(include_known=include_known)\n        payload = {\n            "updated_at": datetime.datetime.now().strftime("%Y-%m-%d %H:%M:%S"),\n            "source_catalog_path": viewer.unknown_mod_catalog_path,\n            "entry_count": int(len(entries)),\n            "entries": entries,\n        }\n        with open(path, mode="w", encoding="utf-8") as f:\n            json.dump(payload, f, indent=2)\n        return path\n    except EXPECTED_RUNTIME_ERRORS as e:\n        Py4GW.Console.Log("DropViewer", f"Unknown mod guess export failed: {e}", Py4GW.Console.MessageType.Warning)\n        return ""',
    'auto_export_unknown_mod_artifacts_if_due': 'def _drop_viewer_unknown_mods_impl_auto_export_unknown_mod_artifacts_if_due(viewer, force: bool = False):\n    if not bool(getattr(viewer, "unknown_mod_auto_export_enabled", True)):\n        return\n    if not viewer.unknown_mod_catalog_dirty and not force:\n        return\n\n    should_export_catalog = bool(force)\n    if not should_export_catalog:\n        try:\n            should_export_catalog = bool(viewer.unknown_mod_auto_export_timer.IsExpired())\n        except EXPECTED_RUNTIME_ERRORS:\n            should_export_catalog = True\n    if not should_export_catalog:\n        return\n\n    try:\n        viewer.unknown_mod_auto_export_timer.Reset()\n    except EXPECTED_RUNTIME_ERRORS:\n        pass\n\n    viewer._flush_unknown_mod_catalog_if_dirty(force=True)\n    if not viewer.unknown_mod_catalog:\n        return\n\n    should_export_guess = bool(force)\n    if not should_export_guess:\n        try:\n            should_export_guess = bool(viewer.unknown_mod_guess_auto_export_timer.IsExpired())\n        except EXPECTED_RUNTIME_ERRORS:\n            should_export_guess = True\n    if not should_export_guess:\n        return\n\n    try:\n        viewer.unknown_mod_guess_auto_export_timer.Reset()\n    except EXPECTED_RUNTIME_ERRORS:\n        pass\n\n    viewer._export_unknown_mod_guess_report(include_known=True)',
    'record_unknown_mod_identifiers': 'def _drop_viewer_unknown_mods_impl_record_unknown_mod_identifiers(\n    viewer,\n    raw_mods,\n    snapshot: dict[str, Any] | None = None,\n    source: str = "",\n    suppress_mod_ids: set[int] | None = None,\n):\n    if not raw_mods:\n        return\n    suppressed = {max(0, viewer._safe_int(v, 0)) for v in list(suppress_mod_ids or set())}\n    suppressed = {v for v in suppressed if v > 0}\n    removed_known = False\n    for ident_i in suppressed:\n        key_i = str(ident_i)\n        if key_i in viewer.unknown_mod_catalog:\n            viewer.unknown_mod_catalog.pop(key_i, None)\n            removed_known = True\n        viewer._remove_unknown_mod_pending_note(ident_i)\n    if removed_known:\n        viewer.unknown_mod_catalog_dirty = True\n        viewer._auto_export_unknown_mod_artifacts_if_due(force=True)\n    src = viewer._ensure_text(source).strip() or viewer._ensure_text((snapshot or {}).get("_source", "")).strip() or "unknown"\n    owner_name = viewer._ensure_text((snapshot or {}).get("_owner", "")).strip()\n    item_name = viewer._clean_item_name((snapshot or {}).get("name", ""))\n    event_id = viewer._ensure_text((snapshot or {}).get("event_id", "")).strip()\n    model_id = max(0, viewer._safe_int((snapshot or {}).get("model_id", 0), 0))\n    item_type = max(0, viewer._safe_int((snapshot or {}).get("item_type", 0), 0))\n    co_mod_ids = []\n    try:\n        co_mod_ids = sorted({\n            max(0, int(mod[0]))\n            for mod in list(raw_mods or [])\n            if isinstance(mod, (list, tuple)) and len(mod) >= 3\n        })\n        co_mod_ids = [mid for mid in co_mod_ids if mid > 0]\n    except EXPECTED_RUNTIME_ERRORS:\n        co_mod_ids = []\n\n    now_ts = time.time()\n    ttl = max(2.0, float(viewer.unknown_mod_recent_ttl_s))\n    try:\n        viewer.unknown_mod_recent_seen = {\n            key: ts\n            for key, ts in viewer.unknown_mod_recent_seen.items()\n            if (now_ts - float(ts)) <= ttl\n        }\n    except EXPECTED_RUNTIME_ERRORS:\n        viewer.unknown_mod_recent_seen = {}\n\n    changed = False\n    was_dirty_before = bool(viewer.unknown_mod_catalog_dirty)\n    for mod in list(raw_mods or []):\n        if not isinstance(mod, (list, tuple)) or len(mod) < 3:\n            continue\n        ident = max(0, viewer._safe_int(mod[0], 0))\n        if ident in suppressed:\n            viewer._remove_unknown_mod_pending_note(ident)\n            continue\n        if viewer._is_known_or_excluded_mod_id(ident):\n            continue\n        arg1 = viewer._safe_int(mod[1], 0)\n        arg2 = viewer._safe_int(mod[2], 0)\n        dedupe_key = f"{src}|{model_id}|{item_type}|{ident}|{arg1}|{arg2}|{event_id}"\n        if dedupe_key in viewer.unknown_mod_recent_seen:\n            continue\n        viewer.unknown_mod_recent_seen[dedupe_key] = now_ts\n\n        key = str(ident)\n        is_new_mod_id = key not in viewer.unknown_mod_catalog\n        entry = viewer._normalize_unknown_mod_entry(viewer.unknown_mod_catalog.get(key, {}))\n        entry["count"] = max(0, int(entry.get("count", 0))) + 1\n\n        arg1_min = entry.get("arg1_min", None)\n        arg1_max = entry.get("arg1_max", None)\n        arg2_min = entry.get("arg2_min", None)\n        arg2_max = entry.get("arg2_max", None)\n        entry["arg1_min"] = arg1 if arg1_min is None else min(int(arg1_min), int(arg1))\n        entry["arg1_max"] = arg1 if arg1_max is None else max(int(arg1_max), int(arg1))\n        entry["arg2_min"] = arg2 if arg2_min is None else min(int(arg2_min), int(arg2))\n        entry["arg2_max"] = arg2 if arg2_max is None else max(int(arg2_max), int(arg2))\n\n        item_types = list(entry.get("item_types", []) or [])\n        if item_type > 0 and item_type not in item_types and len(item_types) < 24:\n            item_types.append(item_type)\n        entry["item_types"] = sorted(item_types)\n\n        model_ids = list(entry.get("model_ids", []) or [])\n        if model_id > 0 and model_id not in model_ids and len(model_ids) < 40:\n            model_ids.append(model_id)\n        entry["model_ids"] = sorted(model_ids)\n\n        co_ids = [mid for mid in co_mod_ids if mid != ident]\n        existing_co = list(entry.get("co_mod_ids", []) or [])\n        for co_id in co_ids:\n            if co_id not in existing_co:\n                existing_co.append(co_id)\n            if len(existing_co) >= 40:\n                break\n        entry["co_mod_ids"] = sorted(existing_co)\n\n        sample_items = list(entry.get("sample_items", []) or [])\n        if item_name and item_name not in sample_items and len(sample_items) < 12:\n            sample_items.append(item_name)\n        entry["sample_items"] = sample_items\n\n        sample_events = list(entry.get("sample_events", []) or [])\n        if event_id and event_id not in sample_events and len(sample_events) < 12:\n            sample_events.append(event_id)\n        entry["sample_events"] = sample_events\n\n        source_map = dict(entry.get("sources", {}) or {})\n        source_map[src] = max(0, int(source_map.get(src, 0))) + 1\n        entry["sources"] = source_map\n        entry["last_seen"] = datetime.datetime.now().strftime("%Y-%m-%d %H:%M:%S")\n        viewer.unknown_mod_catalog[key] = entry\n        if is_new_mod_id:\n            viewer._notify_new_unknown_mod_discovered(\n                ident=ident,\n                arg1=arg1,\n                arg2=arg2,\n                owner_name=owner_name,\n                item_name=item_name,\n                item_type=item_type,\n                model_id=model_id,\n                source=src,\n            )\n        changed = True\n\n    if changed:\n        viewer.unknown_mod_catalog_dirty = True\n        viewer._auto_export_unknown_mod_artifacts_if_due(force=not was_dirty_before)',
    'export_unknown_mod_catalog': 'def _drop_viewer_unknown_mods_impl_export_unknown_mod_catalog(viewer) -> str:\n    viewer._flush_unknown_mod_catalog_if_dirty(force=True)\n    path = viewer._ensure_text(viewer.unknown_mod_catalog_path).strip()\n    if path and os.path.exists(path):\n        return path\n    if viewer._save_unknown_mod_catalog():\n        viewer.unknown_mod_catalog_dirty = False\n        return path\n    return ""',
}


def load_mod_database(viewer, *args, **kwargs):
    return _bind_impl(viewer, 'load_mod_database')(viewer, *args, **kwargs)

def collect_known_mod_ids(viewer, *args, **kwargs):
    return _bind_impl(viewer, 'collect_known_mod_ids')(viewer, *args, **kwargs)

def normalize_unknown_mod_entry(viewer, *args, **kwargs):
    return _bind_impl(viewer, 'normalize_unknown_mod_entry')(viewer, *args, **kwargs)

def load_unknown_mod_catalog(viewer, *args, **kwargs):
    return _bind_impl(viewer, 'load_unknown_mod_catalog')(viewer, *args, **kwargs)

def load_unknown_mod_name_map(viewer, *args, **kwargs):
    return _bind_impl(viewer, 'load_unknown_mod_name_map')(viewer, *args, **kwargs)

def load_unknown_mod_pending_notes(viewer, *args, **kwargs):
    return _bind_impl(viewer, 'load_unknown_mod_pending_notes')(viewer, *args, **kwargs)

def save_unknown_mod_pending_notes(viewer, *args, **kwargs):
    return _bind_impl(viewer, 'save_unknown_mod_pending_notes')(viewer, *args, **kwargs)

def upsert_unknown_mod_pending_note(viewer, *args, **kwargs):
    return _bind_impl(viewer, 'upsert_unknown_mod_pending_note')(viewer, *args, **kwargs)

def remove_unknown_mod_pending_note(viewer, *args, **kwargs):
    return _bind_impl(viewer, 'remove_unknown_mod_pending_note')(viewer, *args, **kwargs)

def unknown_mod_pending_lines(viewer, *args, **kwargs):
    return _bind_impl(viewer, 'unknown_mod_pending_lines')(viewer, *args, **kwargs)

def unknown_mod_pending_count(viewer, *args, **kwargs):
    return _bind_impl(viewer, 'unknown_mod_pending_count')(viewer, *args, **kwargs)

def save_unknown_mod_name_map(viewer, *args, **kwargs):
    return _bind_impl(viewer, 'save_unknown_mod_name_map')(viewer, *args, **kwargs)

def set_unknown_mod_custom_name(viewer, *args, **kwargs):
    return _bind_impl(viewer, 'set_unknown_mod_custom_name')(viewer, *args, **kwargs)

def get_unknown_mod_custom_name(viewer, *args, **kwargs):
    return _bind_impl(viewer, 'get_unknown_mod_custom_name')(viewer, *args, **kwargs)

def unknown_mod_name_map_summary_lines(viewer, *args, **kwargs):
    return _bind_impl(viewer, 'unknown_mod_name_map_summary_lines')(viewer, *args, **kwargs)

def collect_manual_named_mod_lines(viewer, *args, **kwargs):
    return _bind_impl(viewer, 'collect_manual_named_mod_lines')(viewer, *args, **kwargs)

def save_unknown_mod_catalog(viewer, *args, **kwargs):
    return _bind_impl(viewer, 'save_unknown_mod_catalog')(viewer, *args, **kwargs)

def flush_unknown_mod_catalog_if_dirty(viewer, *args, **kwargs):
    return _bind_impl(viewer, 'flush_unknown_mod_catalog_if_dirty')(viewer, *args, **kwargs)

def get_unknown_mod_count(viewer, *args, **kwargs):
    return _bind_impl(viewer, 'get_unknown_mod_count')(viewer, *args, **kwargs)

def is_known_or_excluded_mod_id(viewer, *args, **kwargs):
    return _bind_impl(viewer, 'is_known_or_excluded_mod_id')(viewer, *args, **kwargs)

def unknown_mod_item_type_labels(viewer, *args, **kwargs):
    return _bind_impl(viewer, 'unknown_mod_item_type_labels')(viewer, *args, **kwargs)

def unknown_mod_summary_lines(viewer, *args, **kwargs):
    return _bind_impl(viewer, 'unknown_mod_summary_lines')(viewer, *args, **kwargs)

def push_unknown_mod_notification(viewer, *args, **kwargs):
    return _bind_impl(viewer, 'push_unknown_mod_notification')(viewer, *args, **kwargs)

def unknown_mod_notification_lines(viewer, *args, **kwargs):
    return _bind_impl(viewer, 'unknown_mod_notification_lines')(viewer, *args, **kwargs)

def notify_new_unknown_mod_discovered(viewer, *args, **kwargs):
    return _bind_impl(viewer, 'notify_new_unknown_mod_discovered')(viewer, *args, **kwargs)

def guess_unknown_mod_entry(viewer, *args, **kwargs):
    return _bind_impl(viewer, 'guess_unknown_mod_entry')(viewer, *args, **kwargs)

def unknown_mod_guess_report_entries(viewer, *args, **kwargs):
    return _bind_impl(viewer, 'unknown_mod_guess_report_entries')(viewer, *args, **kwargs)

def get_unknown_mod_unresolved_count(viewer, *args, **kwargs):
    return _bind_impl(viewer, 'get_unknown_mod_unresolved_count')(viewer, *args, **kwargs)

def unknown_mod_guess_summary_lines(viewer, *args, **kwargs):
    return _bind_impl(viewer, 'unknown_mod_guess_summary_lines')(viewer, *args, **kwargs)

def export_unknown_mod_guess_report(viewer, *args, **kwargs):
    return _bind_impl(viewer, 'export_unknown_mod_guess_report')(viewer, *args, **kwargs)

def auto_export_unknown_mod_artifacts_if_due(viewer, *args, **kwargs):
    return _bind_impl(viewer, 'auto_export_unknown_mod_artifacts_if_due')(viewer, *args, **kwargs)

def record_unknown_mod_identifiers(viewer, *args, **kwargs):
    return _bind_impl(viewer, 'record_unknown_mod_identifiers')(viewer, *args, **kwargs)

def export_unknown_mod_catalog(viewer, *args, **kwargs):
    return _bind_impl(viewer, 'export_unknown_mod_catalog')(viewer, *args, **kwargs)
