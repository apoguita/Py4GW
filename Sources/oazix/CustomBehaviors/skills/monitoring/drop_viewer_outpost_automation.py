import sys

EXPECTED_RUNTIME_ERRORS = (TypeError, ValueError, RuntimeError, AttributeError, IndexError, KeyError, OSError)


def _viewer_runtime_module(viewer):
    try:
        return sys.modules.get(viewer.__class__.__module__)
    except EXPECTED_RUNTIME_ERRORS:
        return None


def _bind_impl(viewer, export_name: str):
    module = _viewer_runtime_module(viewer)
    if module is None:
        raise RuntimeError("viewer runtime module unavailable")
    impl_name = f"_outpost_impl_{export_name}"
    if hasattr(module, impl_name):
        return getattr(module, impl_name)
    source = _SOURCES[export_name]
    exec(source, module.__dict__, module.__dict__)
    return getattr(module, impl_name)


_SOURCES = {
    'get_nearby_merchant_candidate_agent_ids': 'def _outpost_impl_get_nearby_merchant_candidate_agent_ids(viewer) -> list[int]:\n    configured_names = [viewer._ensure_text(v).strip() for v in list(getattr(viewer, "auto_buy_kits_merchant_names", []) or []) if viewer._ensure_text(v).strip()]\n    if not configured_names:\n        viewer.auto_buy_kits_last_seen_npc_names = []\n        viewer.auto_buy_kits_last_seen_npc_models = []\n        return []\n    configured_norm = [viewer._normalize_name_for_compare(v) for v in configured_names]\n\n    def _extract_base_name(full_name: str) -> str:\n        # "Volsung [Merchant]" -> "Volsung"\n        raw = viewer._ensure_text(full_name).strip()\n        if not raw:\n            return ""\n        bracket_pos = raw.find("[")\n        if bracket_pos > 0:\n            raw = raw[:bracket_pos].strip()\n        return raw\n\n    def _agent_name(agent_id: int) -> str:\n        try:\n            agent_name = viewer._ensure_text(encoded_wstr_to_str(PyAgent.PyAgent.GetNameByID(int(agent_id)))).strip()\n            if agent_name:\n                return agent_name\n        except EXPECTED_RUNTIME_ERRORS:\n            pass\n        try:\n            agent_name = viewer._ensure_text(Agent.GetNameByID(int(agent_id))).strip()\n            if "feature disabled" in agent_name.lower():\n                return ""\n            return agent_name\n        except EXPECTED_RUNTIME_ERRORS:\n            return ""\n\n    npc_ids = []\n    neutral_ids = []\n    all_agents_ids = []\n    try:\n        npc_ids = list(AgentArray.GetNPCMinipetArray() or [])\n    except EXPECTED_RUNTIME_ERRORS:\n        npc_ids = []\n    try:\n        neutral_ids = list(AgentArray.GetNeutralArray() or [])\n    except EXPECTED_RUNTIME_ERRORS:\n        neutral_ids = []\n    try:\n        all_agents_ids = list(AgentArray.GetAgentArray() or [])\n    except EXPECTED_RUNTIME_ERRORS:\n        all_agents_ids = []\n\n    all_npc_ids = list(AgentArray.Manipulation.Merge(npc_ids, neutral_ids) or [])\n    if not all_npc_ids and all_agents_ids:\n        all_npc_ids = list(all_agents_ids)\n    elif all_agents_ids and len(all_npc_ids) < 10:\n        # Some maps expose merchants only through the full agent list.\n        all_npc_ids = list(AgentArray.Manipulation.Merge(all_npc_ids, all_agents_ids) or [])\n\n    if not all_npc_ids:\n        viewer.auto_buy_kits_last_seen_npc_names = []\n        viewer.auto_buy_kits_last_seen_npc_models = []\n        return []\n\n    filtered_ids = list(all_npc_ids)\n    try:\n        filtered_ids = list(AgentArray.Filter.ByDistance(filtered_ids, Player.GetXY(), 10000.0) or [])\n    except EXPECTED_RUNTIME_ERRORS:\n        filtered_ids = list(all_npc_ids)\n    if not filtered_ids:\n        filtered_ids = list(all_npc_ids)\n\n    try:\n        player_xy = Player.GetXY()\n    except EXPECTED_RUNTIME_ERRORS:\n        player_xy = (0.0, 0.0)\n\n    matches_by_name_idx: dict[int, list[tuple[float, int]]] = {idx: [] for idx in range(len(configured_names))}\n    seen_base_names: list[str] = []\n    seen_base_names_set: set[str] = set()\n    seen_models: list[str] = []\n    seen_models_set: set[str] = set()\n    map_hint_candidates: list[tuple[float, int]] = []\n    merchant_model_candidates: list[tuple[float, int]] = []\n    trade_probe_candidates: list[tuple[float, int]] = []\n    merchant_model_ids = viewer._get_known_merchant_model_ids()\n    try:\n        map_id_key = str(max(0, viewer._safe_int(Map.GetMapID(), 0)))\n    except EXPECTED_RUNTIME_ERRORS:\n        map_id_key = "0"\n    hints_raw = []\n    try:\n        hints_raw = list((viewer.auto_buy_kits_map_model_hints or {}).get(map_id_key, []) or [])\n    except EXPECTED_RUNTIME_ERRORS:\n        hints_raw = []\n    map_hint_model_ids = {max(0, viewer._safe_int(v, 0)) for v in hints_raw if max(0, viewer._safe_int(v, 0)) > 0}\n    has_strict_map_hints = len(map_hint_model_ids) > 0\n\n    target_id = 0\n    try:\n        target_id = max(0, viewer._safe_int(Player.GetTargetID(), 0))\n    except EXPECTED_RUNTIME_ERRORS:\n        target_id = 0\n    for raw_agent_id in list(filtered_ids or []):\n        aid = max(0, viewer._safe_int(raw_agent_id, 0))\n        if aid <= 0:\n            continue\n        try:\n            if not Agent.IsValid(aid):\n                continue\n        except EXPECTED_RUNTIME_ERRORS:\n            continue\n\n        raw_name = _agent_name(aid)\n        base_name = _extract_base_name(raw_name)\n        if base_name and base_name not in seen_base_names_set:\n            seen_base_names_set.add(base_name)\n            seen_base_names.append(base_name)\n        try:\n            ax, ay = Agent.GetXY(aid)\n            dist_sq = ((float(ax) - float(player_xy[0])) ** 2) + ((float(ay) - float(player_xy[1])) ** 2)\n        except EXPECTED_RUNTIME_ERRORS:\n            dist_sq = float("inf")\n\n        model_id = 0\n        model_name = ""\n        try:\n            model_id = max(0, viewer._safe_int(Agent.GetModelID(aid), 0))\n            model_row = ModelData.get(model_id, {}) if isinstance(ModelData, dict) else {}\n            model_name = viewer._ensure_text(model_row.get("name", "")).strip() if hasattr(model_row, "get") else ""\n        except EXPECTED_RUNTIME_ERRORS:\n            model_id = 0\n            model_name = ""\n        if model_id > 0:\n            model_dbg = f"{model_id}:{model_name or \'?\'}"\n            if model_dbg not in seen_models_set:\n                seen_models_set.add(model_dbg)\n                seen_models.append(model_dbg)\n\n        model_norm = viewer._normalize_name_for_compare(model_name)\n        if model_id in map_hint_model_ids:\n            target_bonus = -0.25 if aid == target_id else 0.0\n            map_hint_candidates.append((float(dist_sq) + target_bonus, int(aid)))\n        if model_id in merchant_model_ids or ("merchant" in model_norm) or ("trader" in model_norm):\n            target_bonus = -0.25 if aid == target_id else 0.0\n            merchant_model_candidates.append((float(dist_sq) + target_bonus, int(aid)))\n\n        # Constrained fallback for builds where NPC names are unavailable.\n        # Keep only likely trade NPCs (non-player + neutral/NPC allegiance).\n        try:\n            is_npc = bool(Agent.IsNPC(aid))\n        except EXPECTED_RUNTIME_ERRORS:\n            is_npc = False\n        alleg_name = ""\n        try:\n            _, alleg_name = Agent.GetAllegiance(aid)\n            alleg_name = viewer._normalize_name_for_compare(alleg_name)\n        except EXPECTED_RUNTIME_ERRORS:\n            alleg_name = ""\n        if is_npc and alleg_name in ("neutral", "npc/minipet"):\n            target_bonus = -0.25 if aid == target_id else 0.0\n            trade_probe_candidates.append((float(dist_sq) + target_bonus, int(aid)))\n\n        # Match against all configured names (exact, normalized, and partial).\n        list_idx = None\n        base_norm = viewer._normalize_name_for_compare(base_name)\n        for idx, cfg_name in enumerate(configured_names):\n            cfg_norm = configured_norm[idx]\n            if base_name == cfg_name:\n                list_idx = idx\n                break\n            if base_norm and cfg_norm:\n                if base_norm == cfg_norm or (cfg_norm in base_norm) or (base_norm in cfg_norm):\n                    list_idx = idx\n                    break\n\n        if list_idx is None:\n            continue\n\n        target_bonus = -0.25 if aid == target_id else 0.0\n        matches_by_name_idx[int(list_idx)].append((float(dist_sq) + target_bonus, int(aid)))\n\n    viewer.auto_buy_kits_last_seen_npc_names = seen_base_names[:20]\n    viewer.auto_buy_kits_last_seen_npc_models = seen_models[:20]\n\n    # Strict behavior: first configured merchant name that exists on map wins.\n    ordered: list[int] = []\n    for idx in range(len(configured_names)):\n        rows = list(matches_by_name_idx.get(idx, []) or [])\n        if not rows:\n            continue\n        rows.sort(key=lambda row: row[0])\n        ordered = [int(aid) for _, aid in rows if int(aid) > 0]\n        if ordered:\n            break\n\n    hint_near_ids: list[int] = []\n    hint_far_ids: list[int] = []\n    if map_hint_candidates:\n        map_hint_candidates.sort(key=lambda row: row[0])\n        # Avoid forcing very far stale map hints first (causes long idle in big outposts).\n        # Keep close hints prioritized, defer far hints behind nearby candidates.\n        for dist_sq, aid in map_hint_candidates:\n            a = int(aid)\n            if a <= 0:\n                continue\n            # 2,500 range split between "near" vs "far" hinted targets.\n            if float(dist_sq) <= (2500.0 * 2500.0):\n                hint_near_ids.append(a)\n            else:\n                hint_far_ids.append(a)\n        # Strict behavior requested: when map hints exist, only use those hinted models.\n        strict_hint_only_ids = hint_near_ids + hint_far_ids\n        if strict_hint_only_ids:\n            deduped_hint_ids: list[int] = []\n            seen_hint_ids: set[int] = set()\n            for a in strict_hint_only_ids:\n                if a in seen_hint_ids:\n                    continue\n                seen_hint_ids.add(a)\n                deduped_hint_ids.append(int(a))\n            return deduped_hint_ids[:30]\n    if has_strict_map_hints:\n        # Strict mode for hinted maps: if hinted model isn\'t currently visible, do not\n        # fall back to arbitrary NPCs (prevents wandering to wrong NPC types).\n        return []\n\n    merchant_model_ids_ordered: list[int] = []\n    if merchant_model_candidates:\n        merchant_model_candidates.sort(key=lambda row: row[0])\n        merchant_model_ids_ordered = [int(aid) for _, aid in merchant_model_candidates if int(aid) > 0]\n\n    trade_probe_ids: list[int] = []\n    if trade_probe_candidates:\n        trade_probe_candidates.sort(key=lambda row: row[0])\n        # Keep this constrained: only a small nearest fallback set.\n        trade_probe_ids = [int(aid) for _, aid in trade_probe_candidates[:12] if int(aid) > 0]\n\n    # Priority order:\n    # 1) nearby map hints (explicitly learned/provided)\n    # 2) configured-name matches\n    # 3) known merchant/trader model IDs\n    # 4) constrained nearby NPC probes (last-resort)\n    # 5) far map hints\n    merged: list[int] = []\n    seen: set[int] = set()\n    for aid in hint_near_ids + ordered + merchant_model_ids_ordered + trade_probe_ids + hint_far_ids:\n        if aid in seen:\n            continue\n        seen.add(aid)\n        merged.append(int(aid))\n    return merged[:30]',
    'find_nearby_merchant_agent_id': 'def _outpost_impl_find_nearby_merchant_agent_id(viewer) -> int:\n    candidates = viewer._get_nearby_merchant_candidate_agent_ids()\n    if not candidates:\n        return 0\n    return int(max(0, viewer._safe_int(candidates[0], 0)))',
    'is_merchant_frame_open': 'def _outpost_impl_is_merchant_frame_open(viewer) -> bool:\n    merchant_frame_hash = 3613855137\n    try:\n        merchant_frame_id = UIManager.GetFrameIDByHash(merchant_frame_hash)\n        return bool(UIManager.FrameExists(merchant_frame_id))\n    except EXPECTED_RUNTIME_ERRORS:\n        return False',
    'handle_lions_arch_merchant_dialog_if_visible': 'def _outpost_impl_handle_lions_arch_merchant_dialog_if_visible(viewer) -> bool:\n    # Lions Arch merchant flow can require selecting dialog option:\n    # "Let me see what you\'ve got" before trade window opens.\n    try:\n        if max(0, viewer._safe_int(Map.GetMapID(), 0)) != 55:\n            return False\n        if not bool(Map.IsOutpost()):\n            return False\n        if viewer._is_merchant_frame_open():\n            return False\n    except EXPECTED_RUNTIME_ERRORS:\n        return False\n\n    try:\n        if not bool(UIManager.IsNPCDialogVisible()):\n            return False\n    except EXPECTED_RUNTIME_ERRORS:\n        return False\n\n    try:\n        count = max(0, viewer._safe_int(UIManager.GetDialogButtonCount(False), 0))\n    except EXPECTED_RUNTIME_ERRORS:\n        count = 0\n\n    clicked = False\n    try:\n        if count >= 2:\n            clicked = bool(UIManager.ClickDialogButton(2, False))\n            if clicked:\n                viewer._trace_auto_buy_kits("lions_arch_dialog: clicked option #2 (trade)")\n    except EXPECTED_RUNTIME_ERRORS:\n        clicked = False\n\n    # Fallback if dialog ordering differs.\n    if not clicked:\n        try:\n            clicked = bool(UIManager.ClickDialogButton(1, False))\n            if clicked:\n                viewer._trace_auto_buy_kits("lions_arch_dialog: clicked fallback option #1")\n        except EXPECTED_RUNTIME_ERRORS:\n            clicked = False\n\n    return clicked',
    'get_offered_merchant_items': 'def _outpost_impl_get_offered_merchant_items(viewer) -> list[int]:\n    try:\n        offered = list(Trading.Trader.GetOfferedItems() or [])\n        if offered:\n            return [int(v) for v in offered if int(v) > 0]\n    except EXPECTED_RUNTIME_ERRORS:\n        pass\n    try:\n        offered = list(GLOBAL_CACHE.Trading.Merchant.GetOfferedItems() or [])\n        return [int(v) for v in offered if int(v) > 0]\n    except EXPECTED_RUNTIME_ERRORS:\n        return []',
    'find_merchant_item_by_model': 'def _outpost_impl_find_merchant_item_by_model(viewer, model_id: int) -> int:\n    target_model = max(0, viewer._safe_int(model_id, 0))\n    if target_model <= 0:\n        return 0\n    for offered_item_id in viewer._get_offered_merchant_items():\n        try:\n            if int(Item.GetModelID(offered_item_id)) == target_model:\n                return int(offered_item_id)\n        except EXPECTED_RUNTIME_ERRORS:\n            continue\n    return 0',
    'build_auto_buy_kits_merchant_debug_report': 'def _outpost_impl_build_auto_buy_kits_merchant_debug_report(viewer) -> str:\n    ts = datetime.datetime.now().strftime("%Y-%m-%d %H:%M:%S")\n    lines: list[str] = [f"Merchant Scan Debug @ {ts}"]\n    map_id = 0\n\n    try:\n        map_id = max(0, viewer._safe_int(Map.GetMapID(), 0))\n        map_name = viewer._ensure_text(Map.GetMapName(map_id)).strip() if map_id > 0 else ""\n        lines.append(f"Map: {map_id} {map_name}")\n    except EXPECTED_RUNTIME_ERRORS:\n        lines.append("Map: <unavailable>")\n    try:\n        lines.append(f"Outpost: {bool(Map.IsOutpost())}")\n    except EXPECTED_RUNTIME_ERRORS:\n        lines.append("Outpost: <unavailable>")\n\n    try:\n        px, py = Player.GetXY()\n        lines.append(f"Player XY: ({float(px):.1f}, {float(py):.1f})")\n    except EXPECTED_RUNTIME_ERRORS:\n        px, py = (0.0, 0.0)\n        lines.append("Player XY: <unavailable>")\n\n    configured_names = [viewer._ensure_text(v).strip() for v in list(getattr(viewer, "auto_buy_kits_merchant_names", []) or []) if viewer._ensure_text(v).strip()]\n    lines.append("Configured merchant names:")\n    if configured_names:\n        for idx, name in enumerate(configured_names, start=1):\n            lines.append(f"  {idx}. {name}")\n    else:\n        lines.append("  <empty>")\n\n    npc_ids = []\n    neutral_ids = []\n    all_agent_ids = []\n    try:\n        npc_ids = list(AgentArray.GetNPCMinipetArray() or [])\n    except EXPECTED_RUNTIME_ERRORS:\n        npc_ids = []\n    try:\n        neutral_ids = list(AgentArray.GetNeutralArray() or [])\n    except EXPECTED_RUNTIME_ERRORS:\n        neutral_ids = []\n    try:\n        all_agent_ids = list(AgentArray.GetAgentArray() or [])\n    except EXPECTED_RUNTIME_ERRORS:\n        all_agent_ids = []\n\n    merged_ids = list(AgentArray.Manipulation.Merge(npc_ids, neutral_ids) or [])\n    if not merged_ids and all_agent_ids:\n        merged_ids = list(all_agent_ids)\n    elif all_agent_ids and len(merged_ids) < 10:\n        merged_ids = list(AgentArray.Manipulation.Merge(merged_ids, all_agent_ids) or [])\n\n    filtered_ids = list(merged_ids)\n    try:\n        filtered_ids = list(AgentArray.Filter.ByDistance(filtered_ids, (px, py), 10000.0) or [])\n    except EXPECTED_RUNTIME_ERRORS:\n        filtered_ids = list(merged_ids)\n    if not filtered_ids:\n        filtered_ids = list(merged_ids)\n\n    lines.append(f"Scan counts: npc={len(npc_ids)} neutral={len(neutral_ids)} all_agents={len(all_agent_ids)} merged={len(merged_ids)} filtered={len(filtered_ids)}")\n    known_model_ids = viewer._get_known_merchant_model_ids()\n    lines.append(f"Known merchant/trader model IDs: {len(known_model_ids)}")\n    map_hint_ids = []\n    try:\n        map_hint_ids = list((viewer.auto_buy_kits_map_model_hints or {}).get(str(map_id), []) or [])\n    except EXPECTED_RUNTIME_ERRORS:\n        map_hint_ids = []\n    lines.append(f"Map model hints: {\', \'.join([str(int(v)) for v in map_hint_ids]) if map_hint_ids else \'<none>\'}")\n\n    configured_norm = [viewer._normalize_name_for_compare(v) for v in configured_names]\n\n    def _extract_base_name(full_name: str) -> str:\n        raw = viewer._ensure_text(full_name).strip()\n        if not raw:\n            return ""\n        bracket_pos = raw.find("[")\n        if bracket_pos > 0:\n            raw = raw[:bracket_pos].strip()\n        return raw\n\n    def _name_from_pyagent(agent_id: int) -> str:\n        try:\n            return viewer._ensure_text(encoded_wstr_to_str(PyAgent.PyAgent.GetNameByID(int(agent_id)))).strip()\n        except EXPECTED_RUNTIME_ERRORS:\n            return ""\n\n    def _name_from_agent(agent_id: int) -> str:\n        try:\n            return viewer._ensure_text(Agent.GetNameByID(int(agent_id))).strip()\n        except EXPECTED_RUNTIME_ERRORS:\n            return ""\n\n    def _first_list_match(base_name: str) -> int:\n        base_norm = viewer._normalize_name_for_compare(base_name)\n        for idx, cfg_name in enumerate(configured_names):\n            cfg_norm = configured_norm[idx]\n            if base_name == cfg_name:\n                return idx\n            if base_norm and cfg_norm and (base_norm == cfg_norm or (cfg_norm in base_norm) or (base_norm in cfg_norm)):\n                return idx\n        return -1\n\n    lines.append("NPC rows:")\n    for raw_agent_id in list(filtered_ids or []):\n        aid = max(0, viewer._safe_int(raw_agent_id, 0))\n        if aid <= 0:\n            continue\n        try:\n            is_valid = bool(Agent.IsValid(aid))\n        except EXPECTED_RUNTIME_ERRORS:\n            is_valid = False\n        if not is_valid:\n            continue\n\n        try:\n            ax, ay = Agent.GetXY(aid)\n            dist = (((float(ax) - float(px)) ** 2) + ((float(ay) - float(py)) ** 2)) ** 0.5\n        except EXPECTED_RUNTIME_ERRORS:\n            ax, ay, dist = (0.0, 0.0, 999999.0)\n\n        name_py = _name_from_pyagent(aid)\n        name_agent = _name_from_agent(aid)\n        base_name = _extract_base_name(name_py or name_agent)\n\n        model_id = 0\n        model_name = ""\n        try:\n            model_id = max(0, viewer._safe_int(Agent.GetModelID(aid), 0))\n            if isinstance(ModelData, dict):\n                row = ModelData.get(model_id, {})\n                if isinstance(row, dict):\n                    model_name = viewer._ensure_text(row.get("name", "")).strip()\n        except EXPECTED_RUNTIME_ERRORS:\n            model_id = 0\n            model_name = ""\n\n        is_npc = False\n        try:\n            is_npc = bool(Agent.IsNPC(aid))\n        except EXPECTED_RUNTIME_ERRORS:\n            is_npc = False\n        alleg_name = "?"\n        try:\n            _, alleg_name = Agent.GetAllegiance(aid)\n        except EXPECTED_RUNTIME_ERRORS:\n            alleg_name = "?"\n\n        list_idx = _first_list_match(base_name)\n        list_match = configured_names[list_idx] if 0 <= list_idx < len(configured_names) else "-"\n        lines.append(\n            f"  aid={aid} dist={dist:.1f} xy=({float(ax):.1f},{float(ay):.1f}) "\n            f"model={model_id}:{model_name or \'?\'} base=\'{base_name}\' "\n            f"py=\'{name_py}\' agent=\'{name_agent}\' is_npc={is_npc} allegiance=\'{alleg_name}\' "\n            f"known_model={model_id in known_model_ids} list_match={list_match}"\n        )\n\n    candidates = list(viewer._get_nearby_merchant_candidate_agent_ids() or [])\n    lines.append(f"Chosen candidates: {\', \'.join([str(int(v)) for v in candidates]) if candidates else \'<none>\'}")\n    lines.append(f"Cached seen names: {\', \'.join(list(getattr(viewer, \'auto_buy_kits_last_seen_npc_names\', []) or [])) or \'<none>\'}")\n    lines.append(f"Cached seen models: {\', \'.join(list(getattr(viewer, \'auto_buy_kits_last_seen_npc_models\', []) or [])) or \'<none>\'}")\n    lines.append("AutoBuyKits trace:")\n    trace_rows = list(getattr(viewer, "auto_buy_kits_debug_trace", []) or [])\n    if trace_rows:\n        for row in trace_rows[-60:]:\n            lines.append(f"  {viewer._ensure_text(row)}")\n    else:\n        lines.append("  <none>")\n    return "\\n".join(lines)',
    'dump_auto_buy_kits_merchant_debug_report': 'def _outpost_impl_dump_auto_buy_kits_merchant_debug_report(viewer) -> int:\n    try:\n        report = viewer._build_auto_buy_kits_merchant_debug_report()\n        debug_dir = os.path.join(os.path.dirname(constants.DROP_LOG_PATH), "merchant_debug")\n        os.makedirs(debug_dir, exist_ok=True)\n        ts = datetime.datetime.now().strftime("%Y%m%d_%H%M%S")\n        out_path = os.path.join(debug_dir, f"merchant_scan_{ts}.txt")\n        with open(out_path, mode="w", encoding="utf-8") as f:\n            f.write(report)\n        clip_state = "file only"\n        try:\n            PyImGui.set_clipboard_text(report)\n            clip_state = "copied to clipboard"\n        except EXPECTED_RUNTIME_ERRORS:\n            clip_state = "clipboard failed"\n        viewer.set_status(f"Merchant debug dumped: {out_path} ({clip_state})")\n        return 1\n    except EXPECTED_RUNTIME_ERRORS as e:\n        viewer.set_status(f"Merchant debug dump failed: {e}")\n        return 0',
    'build_inventory_slot_order': 'def _outpost_impl_build_inventory_slot_order(viewer) -> list[tuple[int, int]]:\n    slot_order: list[tuple[int, int]] = []\n    try:\n        bag_enums = ItemArray.CreateBagList(1, 2, 3, 4)\n    except EXPECTED_RUNTIME_ERRORS:\n        bag_enums = []\n    for bag_enum in list(bag_enums or []):\n        try:\n            bag_id = int(getattr(bag_enum, "value", 0))\n            bag_name = viewer._ensure_text(getattr(bag_enum, "name", ""))\n            if bag_id <= 0:\n                continue\n            bag_instance = PyInventory.Bag(bag_id, bag_name)\n            bag_size = max(0, int(bag_instance.GetSize()))\n        except EXPECTED_RUNTIME_ERRORS:\n            continue\n        for slot_idx in range(bag_size):\n            slot_order.append((bag_id, int(slot_idx)))\n    return slot_order',
    'collect_kit_item_ids_for_sort': 'def _outpost_impl_collect_kit_item_ids_for_sort(viewer) -> list[int]:\n    try:\n        salvage_kit_model = int(ModelID.Salvage_Kit.value)\n    except EXPECTED_RUNTIME_ERRORS:\n        salvage_kit_model = 2992\n    try:\n        id_kit_model = int(ModelID.Identification_Kit.value)\n    except EXPECTED_RUNTIME_ERRORS:\n        id_kit_model = 2989\n    try:\n        superior_id_model = int(ModelID.Superior_Identification_Kit.value)\n    except EXPECTED_RUNTIME_ERRORS:\n        superior_id_model = 5899\n    try:\n        bags_to_check = ItemArray.CreateBagList(1, 2, 3, 4)\n        item_array = list(ItemArray.GetItemArray(bags_to_check) or [])\n    except EXPECTED_RUNTIME_ERRORS:\n        item_array = []\n\n    kit_item_ids: list[int] = []\n    for raw_item_id in item_array:\n        item_id = max(0, viewer._safe_int(raw_item_id, 0))\n        if item_id <= 0:\n            continue\n        try:\n            model_id = int(Item.GetModelID(item_id))\n        except EXPECTED_RUNTIME_ERRORS:\n            continue\n        if model_id in (salvage_kit_model, superior_id_model, id_kit_model):\n            kit_item_ids.append(item_id)\n\n    def _sort_key(item_id: int):\n        bag_id, slot = Inventory.FindItemBagAndSlot(item_id)\n        return (viewer._safe_int(bag_id, 99), viewer._safe_int(slot, 999))\n\n    try:\n        kit_item_ids.sort(key=_sort_key)\n    except EXPECTED_RUNTIME_ERRORS:\n        pass\n    return kit_item_ids',
    'collect_kit_quantity_map': 'def _outpost_impl_collect_kit_quantity_map(viewer) -> dict[int, int]:\n    quantities: dict[int, int] = {}\n    for item_id in viewer._collect_kit_item_ids_for_sort():\n        try:\n            quantities[int(item_id)] = max(0, viewer._safe_int(Item.Properties.GetQuantity(int(item_id)), 0))\n        except EXPECTED_RUNTIME_ERRORS:\n            quantities[int(item_id)] = 0\n    return quantities',
    'is_rune_item_for_reorder': 'def _outpost_impl_is_rune_item_for_reorder(viewer, item_id: int) -> bool:\n    try:\n        _, item_type_name = Item.GetItemType(int(item_id))\n        type_text = viewer._ensure_text(item_type_name).strip().lower()\n        return "rune" in type_text\n    except EXPECTED_RUNTIME_ERRORS:\n        return False',
    'inventory_reorder_bucket': 'def _outpost_impl_inventory_reorder_bucket(viewer, item_id: int) -> int:\n    try:\n        _, rarity_name = Item.Rarity.GetRarity(int(item_id))\n        rarity = viewer._ensure_text(rarity_name).strip().lower()\n    except EXPECTED_RUNTIME_ERRORS:\n        rarity = ""\n\n    if rarity == "gold":\n        return 1\n    if rarity == "purple":\n        return 2\n    if rarity == "blue":\n        return 3\n    if rarity == "white":\n        return 4\n    try:\n        if bool(Item.Type.IsTome(int(item_id))):\n            return 5\n    except EXPECTED_RUNTIME_ERRORS:\n        pass\n    if viewer._is_rune_item_for_reorder(int(item_id)):\n        return 6\n    return 7',
    'reorder_inventory_after_kits': 'def _outpost_impl_reorder_inventory_after_kits(viewer) -> Generator[Any, Any, int]:\n    slot_order = viewer._build_inventory_slot_order()\n    if not slot_order:\n        return 0\n\n    try:\n        bags_to_check = ItemArray.CreateBagList(1, 2, 3, 4)\n        all_item_ids = [max(0, viewer._safe_int(v, 0)) for v in list(ItemArray.GetItemArray(bags_to_check) or [])]\n        all_item_ids = [v for v in all_item_ids if v > 0]\n    except EXPECTED_RUNTIME_ERRORS:\n        all_item_ids = []\n    if not all_item_ids:\n        return 0\n\n    kit_item_ids = viewer._collect_kit_item_ids_for_sort()\n    kit_set = set(int(v) for v in list(kit_item_ids or []) if viewer._safe_int(v, 0) > 0)\n    non_kit_item_ids = [int(v) for v in all_item_ids if int(v) not in kit_set]\n    if not non_kit_item_ids:\n        return 0\n\n    target_slots = slot_order[len(kit_set):]\n    if not target_slots:\n        return 0\n\n    def _pos_key(item_id: int) -> tuple[int, int]:\n        bag_id, slot = Inventory.FindItemBagAndSlot(int(item_id))\n        return (viewer._safe_int(bag_id, 99), viewer._safe_int(slot, 999))\n\n    non_kit_item_ids.sort(key=lambda item_id: (viewer._inventory_reorder_bucket(int(item_id)), _pos_key(int(item_id))))\n\n    moved = 0\n    for idx, item_id in enumerate(non_kit_item_ids):\n        if idx >= len(target_slots):\n            break\n        target_bag, target_slot = target_slots[idx]\n        current_bag, current_slot = Inventory.FindItemBagAndSlot(int(item_id))\n        current_bag_i = viewer._safe_int(current_bag, 0)\n        current_slot_i = viewer._safe_int(current_slot, -1)\n        if current_bag_i == target_bag and current_slot_i == target_slot:\n            continue\n        try:\n            qty = max(1, viewer._safe_int(Item.Properties.GetQuantity(int(item_id)), 1))\n            Inventory.MoveItem(int(item_id), int(target_bag), int(target_slot), int(qty))\n            moved += 1\n            yield from Routines.Yield.wait(90)\n        except EXPECTED_RUNTIME_ERRORS:\n            continue\n    return int(moved)',
    'sort_kits_to_front': 'def _outpost_impl_sort_kits_to_front(viewer, priority_item_ids: list[int] | None = None) -> Generator[Any, Any, int]:\n    slot_order = viewer._build_inventory_slot_order()\n    if not slot_order:\n        return 0\n    kit_item_ids = viewer._collect_kit_item_ids_for_sort()\n    if not kit_item_ids:\n        return 0\n    if priority_item_ids:\n        priority_set = {int(v) for v in list(priority_item_ids or []) if viewer._safe_int(v, 0) > 0}\n        prioritized = [item_id for item_id in kit_item_ids if item_id in priority_set]\n        if prioritized:\n            kit_item_ids = prioritized\n\n    moved = 0\n    for idx, kit_item_id in enumerate(kit_item_ids):\n        if idx >= len(slot_order):\n            break\n        target_bag, target_slot = slot_order[idx]\n        current_bag, current_slot = Inventory.FindItemBagAndSlot(kit_item_id)\n        current_bag_i = viewer._safe_int(current_bag, 0)\n        current_slot_i = viewer._safe_int(current_slot, -1)\n        if current_bag_i == target_bag and current_slot_i == target_slot:\n            continue\n        try:\n            Inventory.MoveItem(kit_item_id, target_bag, target_slot, 1)\n            moved += 1\n            yield from Routines.Yield.wait(100)\n        except EXPECTED_RUNTIME_ERRORS:\n            continue\n    return int(moved)',
    'buy_item_from_merchant': 'def _outpost_impl_buy_item_from_merchant(viewer, offered_item_id: int, quantity: int) -> Generator[Any, Any, int]:\n    item_id = max(0, viewer._safe_int(offered_item_id, 0))\n    to_buy = max(0, viewer._safe_int(quantity, 0))\n    if item_id <= 0 or to_buy <= 0:\n        return 0\n\n    bought = 0\n    for _ in range(to_buy):\n        if viewer._should_abort_auto_buy_kits():\n            break\n        # Merchant API expects buy cost (sell value * 2) in many maps/frames.\n        buy_cost = 0\n        try:\n            buy_cost = max(0, int(GLOBAL_CACHE.Item.Properties.GetValue(item_id)) * 2)\n        except EXPECTED_RUNTIME_ERRORS:\n            buy_cost = 0\n        if buy_cost <= 0:\n            # Fallback for edge cases where property value is unavailable.\n            try:\n                GLOBAL_CACHE.Trading.Trader.RequestQuote(item_id)\n                wait_elapsed_ms = 0\n                while wait_elapsed_ms < 1000:\n                    if viewer._should_abort_auto_buy_kits():\n                        break\n                    yield from Routines.Yield.wait(50)\n                    wait_elapsed_ms += 50\n                    quote_value = int(Trading.Trader.GetQuotedValue())\n                    if quote_value >= 0:\n                        buy_cost = quote_value\n                        break\n            except EXPECTED_RUNTIME_ERRORS:\n                buy_cost = 0\n        if buy_cost <= 0:\n            break\n\n        try:\n            GLOBAL_CACHE.Trading.Merchant.BuyItem(item_id, buy_cost)\n        except EXPECTED_RUNTIME_ERRORS:\n            try:\n                GLOBAL_CACHE.Trading.Trader.BuyItem(item_id, buy_cost)\n            except EXPECTED_RUNTIME_ERRORS:\n                break\n\n        # Small settle delay between purchases.\n        yield from Routines.Yield.wait(125)\n        if viewer._should_abort_auto_buy_kits():\n            break\n        bought += 1\n    return int(bought)',
    'approach_and_open_merchant': 'def _outpost_impl_approach_and_open_merchant(viewer, agent_id: int) -> Generator[Any, Any, bool]:\n    if viewer._should_abort_auto_buy_kits():\n        viewer._trace_auto_buy_kits("approach: aborted before start")\n        return False\n    target_agent_id = max(0, viewer._safe_int(agent_id, 0))\n    if target_agent_id <= 0:\n        viewer._trace_auto_buy_kits("approach: invalid target agent id")\n        return False\n\n    try:\n        px, py = Player.GetXY()\n        nx, ny = Agent.GetXY(target_agent_id)\n        dist_sq = ((float(nx) - float(px)) ** 2) + ((float(ny) - float(py)) ** 2)\n        model_id_dbg = 0\n        try:\n            model_id_dbg = max(0, viewer._safe_int(Agent.GetModelID(target_agent_id), 0))\n        except EXPECTED_RUNTIME_ERRORS:\n            model_id_dbg = 0\n        viewer._trace_auto_buy_kits(\n            f"approach: target aid={target_agent_id} model={model_id_dbg} dist={float(dist_sq) ** 0.5:.1f}"\n        )\n        if dist_sq > (165.0 * 165.0):\n            if viewer._should_abort_auto_buy_kits():\n                viewer._trace_auto_buy_kits("approach: aborted before movement")\n                return False\n            moved_via_path = False\n            try:\n                from Py4GWCoreLib.Pathing import AutoPathing\n\n                path3d = yield from AutoPathing().get_path_to(\n                    float(nx),\n                    float(ny),\n                    smooth_by_los=True,\n                    margin=100.0,\n                    step_dist=300.0,\n                )\n                path2d = [(x, y) for (x, y, *_) in list(path3d or [])]\n                if path2d:\n                    viewer._trace_auto_buy_kits(\n                        f"approach: path_to target built (nodes={len(path2d)})"\n                    )\n                    moved_via_path = bool((yield from Routines.Yield.Movement.FollowPath(\n                        path_points=path2d,\n                        custom_exit_condition=lambda: Agent.IsDead(Player.GetAgentID()),\n                        tolerance=150,\n                        log=constants.DEBUG,\n                        timeout=10000,\n                        custom_pause_fn=lambda: False,\n                        stop_on_party_wipe=False,\n                    )))\n                    viewer._trace_auto_buy_kits(\n                        f"approach: FollowPath(path2d) result={moved_via_path}"\n                    )\n                else:\n                    viewer._trace_auto_buy_kits("approach: AutoPathing returned empty path")\n            except EXPECTED_RUNTIME_ERRORS:\n                viewer._trace_auto_buy_kits("approach: AutoPathing/FollowPath(path2d) raised")\n                pass\n            if not moved_via_path:\n                try:\n                    viewer._trace_auto_buy_kits("approach: fallback FollowPath direct-to-NPC")\n                    yield from Routines.Yield.Movement.FollowPath(\n                        path_points=[(float(nx), float(ny))],\n                        custom_exit_condition=lambda: Agent.IsDead(Player.GetAgentID()),\n                        tolerance=175,\n                        log=constants.DEBUG,\n                        timeout=12000,\n                        custom_pause_fn=lambda: False,\n                        stop_on_party_wipe=False,\n                    )\n                    viewer._trace_auto_buy_kits("approach: fallback FollowPath finished")\n                except EXPECTED_RUNTIME_ERRORS:\n                    viewer._trace_auto_buy_kits("approach: fallback FollowPath raised")\n                    pass\n    except EXPECTED_RUNTIME_ERRORS:\n        viewer._trace_auto_buy_kits("approach: failed reading player/target XY")\n        pass\n\n    try:\n        yield from Routines.Yield.Player.ChangeTarget(target_agent_id, log=False)\n        viewer._trace_auto_buy_kits(f"approach: changed target to aid={target_agent_id}")\n    except EXPECTED_RUNTIME_ERRORS:\n        viewer._trace_auto_buy_kits("approach: ChangeTarget raised")\n        pass\n\n    try:\n        yield from Routines.Yield.Player.InteractAgent(target_agent_id, log=False)\n        viewer._trace_auto_buy_kits(f"approach: InteractAgent(aid={target_agent_id}) yielded")\n    except EXPECTED_RUNTIME_ERRORS:\n        viewer._trace_auto_buy_kits("approach: InteractAgent raised")\n        pass\n\n    try:\n        Player.Interact(target_agent_id, call_target=True)\n        viewer._trace_auto_buy_kits(f"approach: Player.Interact(aid={target_agent_id}) queued")\n    except EXPECTED_RUNTIME_ERRORS:\n        viewer._trace_auto_buy_kits("approach: Player.Interact raised")\n        return False\n\n    try:\n        if viewer._handle_lions_arch_merchant_dialog_if_visible():\n            yield from Routines.Yield.wait(250)\n    except EXPECTED_RUNTIME_ERRORS:\n        pass\n\n    wait_elapsed_ms = 0\n    reinteract_elapsed_ms = 0\n    while wait_elapsed_ms < 3500:\n        if viewer._should_abort_auto_buy_kits():\n            viewer._trace_auto_buy_kits("approach: aborted during merchant wait")\n            return False\n        if viewer._is_merchant_frame_open():\n            viewer._trace_auto_buy_kits(f"approach: merchant frame opened after {wait_elapsed_ms}ms")\n            return True\n        try:\n            if viewer._handle_lions_arch_merchant_dialog_if_visible():\n                yield from Routines.Yield.wait(250)\n        except EXPECTED_RUNTIME_ERRORS:\n            pass\n        yield from Routines.Yield.wait(200)\n        wait_elapsed_ms += 200\n        reinteract_elapsed_ms += 200\n        if reinteract_elapsed_ms >= 600:\n            reinteract_elapsed_ms = 0\n            try:\n                Player.Interact(target_agent_id, call_target=True)\n                viewer._trace_auto_buy_kits(f"approach: re-interact aid={target_agent_id}")\n            except EXPECTED_RUNTIME_ERRORS:\n                pass\n    opened = viewer._is_merchant_frame_open()\n    viewer._trace_auto_buy_kits(f"approach: merchant frame final_open={opened} after wait={wait_elapsed_ms}ms")\n    return opened',
    'run_buy_kits_if_needed_job': 'def _outpost_impl_run_buy_kits_if_needed_job(viewer, verbose_status: bool = True) -> Generator[Any, Any, None]:\n    viewer.auto_buy_kits_job_running = True\n    try:\n        viewer._trace_auto_buy_kits("job: started")\n        if viewer._should_abort_auto_buy_kits():\n            viewer._trace_auto_buy_kits("job: aborted before run")\n            if verbose_status:\n                viewer.set_status("Auto Buy Kits: aborted")\n            return\n        if not bool(Map.IsOutpost()):\n            viewer._trace_auto_buy_kits("job: aborted, not in outpost")\n            if verbose_status:\n                viewer.set_status("Auto Buy Kits: only available in outpost/town")\n            return\n        if not viewer._is_auto_buy_kits_allowed_outpost():\n            viewer._trace_auto_buy_kits("job: aborted, outpost not in allowlist")\n            if verbose_status:\n                viewer.set_status("Auto Buy Kits: only enabled in Lions Arch / Granite Citadel")\n            return\n\n        stats = viewer._collect_local_inventory_kit_stats()\n        salvage_uses = max(0, viewer._safe_int(stats.get("salvage_uses", 0), 0))\n        total_id_uses = max(0, viewer._safe_int(viewer._collect_total_identification_uses(), 0))\n        need_salvage = salvage_uses < 25\n        need_id = total_id_uses < 25\n        viewer._trace_auto_buy_kits(\n            f"job: needs salvage={need_salvage} id={need_id} (salvage_uses={salvage_uses}, total_id_uses={total_id_uses})"\n        )\n        if not need_salvage and not need_id:\n            viewer._trace_auto_buy_kits("job: skipped, enough kits/uses")\n            if verbose_status:\n                viewer.set_status("Auto Buy Kits: skipped (>=25 uses for salvage and total ID)")\n            return\n\n        try:\n            salvage_kit_model = int(ModelID.Salvage_Kit.value)\n        except EXPECTED_RUNTIME_ERRORS:\n            salvage_kit_model = 2992\n        try:\n            id_kit_model = int(ModelID.Identification_Kit.value)\n        except EXPECTED_RUNTIME_ERRORS:\n            id_kit_model = 2989\n        try:\n            superior_id_model = int(ModelID.Superior_Identification_Kit.value)\n        except EXPECTED_RUNTIME_ERRORS:\n            superior_id_model = 5899\n\n        merchant_candidates = list(viewer._get_nearby_merchant_candidate_agent_ids() or [])\n        viewer._trace_auto_buy_kits(f"job: initial candidates={\',\'.join([str(int(v)) for v in merchant_candidates]) or \'<none>\'}")\n        if not merchant_candidates:\n            # After zoning, NPC/name data can lag briefly; rescan before failing.\n            wait_elapsed_ms = 0\n            while wait_elapsed_ms < 2000 and not merchant_candidates:\n                if viewer._should_abort_auto_buy_kits():\n                    viewer._trace_auto_buy_kits("job: aborted while rescanning merchant candidates")\n                    if verbose_status:\n                        viewer.set_status("Auto Buy Kits: aborted")\n                    return\n                yield from Routines.Yield.wait(200)\n                wait_elapsed_ms += 200\n                merchant_candidates = list(viewer._get_nearby_merchant_candidate_agent_ids() or [])\n            viewer._trace_auto_buy_kits(\n                f"job: rescan candidates after {wait_elapsed_ms}ms => {\',\'.join([str(int(v)) for v in merchant_candidates]) or \'<none>\'}"\n            )\n        if not merchant_candidates:\n            # Lions Arch special case: move to Bodrus area once, then rescan hinted model.\n            map_id_now = 0\n            try:\n                map_id_now = max(0, viewer._safe_int(Map.GetMapID(), 0))\n            except EXPECTED_RUNTIME_ERRORS:\n                map_id_now = 0\n            if map_id_now == 55:\n                if viewer._should_abort_auto_buy_kits():\n                    viewer._trace_auto_buy_kits("job: aborted before LA bootstrap move")\n                    if verbose_status:\n                        viewer.set_status("Auto Buy Kits: aborted")\n                    return\n                viewer._trace_auto_buy_kits("job: LA bootstrap move to merchant hub (7396,6327)")\n                try:\n                    yield from Routines.Yield.Movement.FollowPath(\n                        path_points=[(7396.0, 6327.0)],\n                        custom_exit_condition=lambda: Agent.IsDead(Player.GetAgentID()),\n                        tolerance=225,\n                        log=constants.DEBUG,\n                        timeout=15000,\n                        custom_pause_fn=lambda: False,\n                        stop_on_party_wipe=False,\n                    )\n                except EXPECTED_RUNTIME_ERRORS:\n                    viewer._trace_auto_buy_kits("job: LA bootstrap FollowPath raised")\n                yield from Routines.Yield.wait(350)\n                merchant_candidates = list(viewer._get_nearby_merchant_candidate_agent_ids() or [])\n                viewer._trace_auto_buy_kits(\n                    f"job: LA post-move candidates={\',\'.join([str(int(v)) for v in merchant_candidates]) or \'<none>\'}"\n                )\n        merchant_agent_id = int(max(0, viewer._safe_int(merchant_candidates[0], 0))) if merchant_candidates else 0\n        if merchant_agent_id <= 0:\n            viewer._trace_auto_buy_kits("job: failed, no candidate merchant agent id")\n            npc_count = 0\n            try:\n                npc_count = len(list(AgentArray.GetNPCMinipetArray() or []))\n            except EXPECTED_RUNTIME_ERRORS:\n                npc_count = 0\n            if verbose_status:\n                configured = ", ".join([viewer._ensure_text(v).strip() for v in list(getattr(viewer, "auto_buy_kits_merchant_names", []) or []) if viewer._ensure_text(v).strip()])\n                seen_names = ", ".join([viewer._ensure_text(v).strip() for v in list(getattr(viewer, "auto_buy_kits_last_seen_npc_names", []) or []) if viewer._ensure_text(v).strip()])\n                seen_models = ", ".join([viewer._ensure_text(v).strip() for v in list(getattr(viewer, "auto_buy_kits_last_seen_npc_models", []) or []) if viewer._ensure_text(v).strip()])\n                if configured:\n                    if seen_names or seen_models:\n                        if seen_models:\n                            viewer.set_status(f"Auto Buy Kits failed: no listed merchant found (NPCs scanned: {npc_count}; list: {configured}; seen: {seen_names or \'-\'}; models: {seen_models})")\n                        else:\n                            viewer.set_status(f"Auto Buy Kits failed: no listed merchant found (NPCs scanned: {npc_count}; list: {configured}; seen: {seen_names})")\n                    else:\n                        viewer.set_status(f"Auto Buy Kits failed: no listed merchant found (NPCs scanned: {npc_count}; list: {configured})")\n                else:\n                    viewer.set_status(f"Auto Buy Kits failed: no merchant list configured (NPCs scanned: {npc_count})")\n            return\n\n        bought_salvage = 0\n        bought_superior = 0\n        bought_regular_id = 0\n        missing_items: set[str] = set()\n        remaining_need_salvage = bool(need_salvage)\n        remaining_need_id = bool(need_id)\n\n        merchant_opened_any = False\n        tried_npcs = 0\n        for candidate_id in merchant_candidates[:30]:\n            if viewer._should_abort_auto_buy_kits():\n                viewer._trace_auto_buy_kits("job: aborted during merchant candidate loop")\n                if verbose_status:\n                    viewer.set_status("Auto Buy Kits: aborted")\n                return\n            cid = int(max(0, viewer._safe_int(candidate_id, 0)))\n            if cid <= 0:\n                continue\n            tried_npcs += 1\n            c_model = 0\n            try:\n                c_model = max(0, viewer._safe_int(Agent.GetModelID(cid), 0))\n            except EXPECTED_RUNTIME_ERRORS:\n                c_model = 0\n            viewer._trace_auto_buy_kits(f"job: trying candidate aid={cid} model={c_model} ({tried_npcs}/{len(merchant_candidates[:30])})")\n            merchant_opened = bool((yield from viewer._approach_and_open_merchant(cid)))\n            viewer._trace_auto_buy_kits(f"job: candidate aid={cid} merchant_opened={merchant_opened}")\n            if not merchant_opened:\n                continue\n            merchant_opened_any = True\n            candidate_bought_any = False\n\n            salvage_offer_id = viewer._find_merchant_item_by_model(salvage_kit_model) if remaining_need_salvage else 0\n            superior_offer_id = viewer._find_merchant_item_by_model(superior_id_model) if remaining_need_id else 0\n            regular_offer_id = viewer._find_merchant_item_by_model(id_kit_model) if remaining_need_id else 0\n            viewer._trace_auto_buy_kits(\n                f"job: offers aid={cid} salvage={salvage_offer_id} superior={superior_offer_id} regular={regular_offer_id}"\n            )\n\n            # Learn this NPC model for current map as soon as it exposes kit offers.\n            if salvage_offer_id > 0 or superior_offer_id > 0 or regular_offer_id > 0:\n                viewer._record_successful_kit_merchant_model(cid)\n\n            if remaining_need_salvage and salvage_offer_id <= 0:\n                missing_items.add("salvage kit")\n            if remaining_need_id and superior_offer_id <= 0 and regular_offer_id <= 0:\n                missing_items.add("superior ID kit (and regular ID kit)")\n\n            if remaining_need_salvage and salvage_offer_id > 0:\n                if viewer._should_abort_auto_buy_kits():\n                    viewer._trace_auto_buy_kits("job: aborted before salvage purchase")\n                    if verbose_status:\n                        viewer.set_status("Auto Buy Kits: aborted")\n                    return\n                bought_now = int((yield from viewer._buy_item_from_merchant(salvage_offer_id, 4)) or 0)\n                viewer._trace_auto_buy_kits(f"job: buy salvage offer={salvage_offer_id} bought={bought_now}")\n                if bought_now > 0:\n                    bought_salvage += int(bought_now)\n                    candidate_bought_any = True\n                    remaining_need_salvage = False\n                    if "salvage kit" in missing_items:\n                        missing_items.discard("salvage kit")\n\n            if remaining_need_id:\n                if viewer._should_abort_auto_buy_kits():\n                    viewer._trace_auto_buy_kits("job: aborted before ID purchase")\n                    if verbose_status:\n                        viewer.set_status("Auto Buy Kits: aborted")\n                    return\n                if superior_offer_id > 0:\n                    bought_now = int((yield from viewer._buy_item_from_merchant(superior_offer_id, 1)) or 0)\n                    viewer._trace_auto_buy_kits(f"job: buy superior offer={superior_offer_id} bought={bought_now}")\n                    if bought_now > 0:\n                        bought_superior += int(bought_now)\n                        candidate_bought_any = True\n                        remaining_need_id = False\n                        if "superior ID kit (and regular ID kit)" in missing_items:\n                            missing_items.discard("superior ID kit (and regular ID kit)")\n                if remaining_need_id and regular_offer_id > 0:\n                    bought_now = int((yield from viewer._buy_item_from_merchant(regular_offer_id, 2)) or 0)\n                    viewer._trace_auto_buy_kits(f"job: buy regular offer={regular_offer_id} bought={bought_now}")\n                    if bought_now > 0:\n                        bought_regular_id += int(bought_now)\n                        candidate_bought_any = True\n                        remaining_need_id = False\n                        if "superior ID kit (and regular ID kit)" in missing_items:\n                            missing_items.discard("superior ID kit (and regular ID kit)")\n\n            if candidate_bought_any:\n                viewer._record_successful_kit_merchant_model(cid)\n\n            if not remaining_need_salvage and not remaining_need_id:\n                break\n\n        if not merchant_opened_any:\n            viewer._trace_auto_buy_kits(f"job: failed, merchant window not opened (tried={tried_npcs})")\n            if verbose_status:\n                viewer.set_status(f"Auto Buy Kits failed: merchant window not opened (NPCs tried: {tried_npcs})")\n            return\n\n        if bought_salvage <= 0 and bought_superior <= 0 and bought_regular_id <= 0:\n            viewer._trace_auto_buy_kits(\n                f"job: failed, no purchases (missing={\',\'.join(sorted(list(missing_items))) if missing_items else \'<none>\'})"\n            )\n            if missing_items:\n                if verbose_status:\n                    viewer.set_status(f"Auto Buy Kits failed: merchant missing {\', \'.join(sorted(list(missing_items)))}")\n            else:\n                if verbose_status:\n                    viewer.set_status("Auto Buy Kits failed: purchase rejected or insufficient gold")\n            return\n\n        status_parts = []\n        if bought_salvage > 0:\n            status_parts.append(f"salvage kits x{bought_salvage}")\n        if bought_superior > 0:\n            status_parts.append(f"superior ID kits x{bought_superior}")\n        if bought_regular_id > 0:\n            status_parts.append(f"ID kits x{bought_regular_id}")\n        # Sorting/reordering runs on outpost-entry tick, not inside buy flow.\n        if missing_items:\n            status_parts.append(f"missing {\', \'.join(sorted(list(missing_items)))}")\n        viewer.set_status(f"Auto Buy Kits: bought {\', \'.join(status_parts)}")\n        viewer._trace_auto_buy_kits(f"job: success => {\', \'.join(status_parts)}")\n    except EXPECTED_RUNTIME_ERRORS as e:\n        viewer._trace_auto_buy_kits(f"job: exception => {e}")\n        if verbose_status:\n            viewer.set_status(f"Auto Buy Kits failed: {e}")\n    finally:\n        viewer._trace_auto_buy_kits("job: finished")\n        viewer.auto_buy_kits_abort_requested = False\n        viewer.auto_buy_kits_job_running = False',
    'queue_buy_kits_if_needed': 'def _outpost_impl_queue_buy_kits_if_needed(viewer, verbose_status: bool = True) -> int:\n    if viewer.auto_buy_kits_job_running:\n        viewer._trace_auto_buy_kits("queue: rejected, already running")\n        if verbose_status:\n            viewer.set_status("Auto Buy Kits: already running")\n        return 0\n    if not viewer._is_auto_buy_kits_allowed_outpost():\n        viewer._trace_auto_buy_kits("queue: rejected, outpost not in allowlist")\n        if verbose_status:\n            viewer.set_status("Auto Buy Kits: only enabled in Lions Arch / Granite Citadel")\n        return 0\n    if viewer.auto_outpost_store_job_running or viewer.auto_id_job_running or viewer.auto_salvage_job_running or viewer.auto_gold_balance_job_running or viewer.auto_inventory_reorder_job_running:\n        viewer._trace_auto_buy_kits("queue: rejected, busy with other inventory job")\n        if verbose_status:\n            viewer.set_status("Auto Buy Kits: busy with another inventory job")\n        return 0\n    try:\n        viewer.auto_buy_kits_abort_requested = False\n        GLOBAL_CACHE.Coroutines.append(viewer._run_buy_kits_if_needed_job(verbose_status=verbose_status))\n        viewer._trace_auto_buy_kits("queue: queued")\n        if verbose_status:\n            viewer.set_status("Auto Buy Kits: started")\n        return 1\n    except EXPECTED_RUNTIME_ERRORS as e:\n        viewer._trace_auto_buy_kits(f"queue: failed => {e}")\n        if verbose_status:\n            viewer.set_status(f"Auto Buy Kits queue failed: {e}")\n        return 0',
    'run_auto_gold_balance_job': 'def _outpost_impl_run_auto_gold_balance_job(viewer, verbose_status: bool = True) -> Generator[Any, Any, None]:\n    viewer.auto_gold_balance_job_running = True\n    target_amount = max(0, int(viewer.auto_gold_balance_target))\n    try:\n        if not bool(Map.IsOutpost()):\n            if verbose_status:\n                viewer.set_status("Auto Gold: only available in outpost/town")\n            return\n        before_char = max(0, viewer._safe_int(GLOBAL_CACHE.Inventory.GetGoldOnCharacter(), 0))\n        before_storage = max(0, viewer._safe_int(GLOBAL_CACHE.Inventory.GetGoldInStorage(), 0))\n        if before_char == target_amount:\n            if verbose_status:\n                viewer.set_status(f"Auto Gold: already balanced at {target_amount}")\n            return\n\n        yield from Routines.Yield.Items.DepositGold(target_amount, log=False)\n        yield from Routines.Yield.wait(125)\n\n        after_char = max(0, viewer._safe_int(GLOBAL_CACHE.Inventory.GetGoldOnCharacter(), 0))\n        after_storage = max(0, viewer._safe_int(GLOBAL_CACHE.Inventory.GetGoldInStorage(), 0))\n        if after_char == before_char and after_storage == before_storage:\n            if verbose_status:\n                viewer.set_status("Auto Gold: no transfer (storage/character limits)")\n            return\n        if verbose_status:\n            viewer.set_status(f"Auto Gold: balanced to {after_char} (target {target_amount})")\n    except EXPECTED_RUNTIME_ERRORS as e:\n        if verbose_status:\n            viewer.set_status(f"Auto Gold failed: {e}")\n    finally:\n        viewer.auto_gold_balance_job_running = False',
    'queue_auto_gold_balance': 'def _outpost_impl_queue_auto_gold_balance(viewer, verbose_status: bool = True) -> int:\n    if viewer.auto_gold_balance_job_running:\n        if verbose_status:\n            viewer.set_status("Auto Gold: already running")\n        return 0\n    if viewer.auto_outpost_store_job_running or viewer.auto_id_job_running or viewer.auto_salvage_job_running or viewer.auto_buy_kits_job_running or viewer.auto_inventory_reorder_job_running:\n        if verbose_status:\n            viewer.set_status("Auto Gold: busy with another inventory job")\n        return 0\n    try:\n        GLOBAL_CACHE.Coroutines.append(viewer._run_auto_gold_balance_job(verbose_status=verbose_status))\n        if verbose_status:\n            viewer.set_status("Auto Gold: started")\n        return 1\n    except EXPECTED_RUNTIME_ERRORS as e:\n        if verbose_status:\n            viewer.set_status(f"Auto Gold queue failed: {e}")\n        return 0',
    'run_auto_gold_balance_once_on_outpost_entry_tick': 'def _outpost_impl_run_auto_gold_balance_once_on_outpost_entry_tick(viewer) -> bool:\n    entry_key = viewer._refresh_auto_outpost_store_entry_key()\n    if not entry_key:\n        return False\n    if not viewer.auto_gold_balance_enabled:\n        return False\n    if viewer.auto_gold_balance_handled_entry_key == entry_key:\n        return False\n    if viewer.auto_outpost_store_job_running or viewer.auto_id_job_running or viewer.auto_salvage_job_running or viewer.auto_buy_kits_job_running or viewer.auto_gold_balance_job_running or viewer.auto_inventory_reorder_job_running:\n        return False\n\n    target_amount = max(0, int(viewer.auto_gold_balance_target))\n    current_gold = max(0, viewer._safe_int(GLOBAL_CACHE.Inventory.GetGoldOnCharacter(), 0))\n    if current_gold == target_amount:\n        viewer.auto_gold_balance_handled_entry_key = entry_key\n        return False\n\n    queued = viewer._queue_auto_gold_balance(verbose_status=False)\n    if queued > 0:\n        viewer.auto_gold_balance_handled_entry_key = entry_key\n        return True\n    return False',
    'run_auto_inventory_reorder_job': 'def _outpost_impl_run_auto_inventory_reorder_job(viewer, verbose_status: bool = True) -> Generator[Any, Any, None]:\n    viewer.auto_inventory_reorder_job_running = True\n    try:\n        if not bool(Map.IsOutpost()):\n            if verbose_status:\n                viewer.set_status("Inventory Sort: only available in outpost/town")\n            return\n        sorted_count = int((yield from viewer._sort_kits_to_front()) or 0)\n        reordered_count = 0\n        # Leader keeps manual non-kit ordering; followers get full reorder.\n        if not viewer._is_leader_client():\n            reordered_count = int((yield from viewer._reorder_inventory_after_kits()) or 0)\n        if verbose_status:\n            parts = []\n            if sorted_count > 0:\n                parts.append(f"kits x{sorted_count}")\n            if reordered_count > 0:\n                parts.append(f"items x{reordered_count}")\n            if not parts:\n                parts.append("no moves needed")\n            viewer.set_status(f"Inventory Sort: {\', \'.join(parts)}")\n    except EXPECTED_RUNTIME_ERRORS as e:\n        if verbose_status:\n            viewer.set_status(f"Inventory Sort failed: {e}")\n    finally:\n        viewer.auto_inventory_reorder_job_running = False',
    'queue_auto_inventory_reorder': 'def _outpost_impl_queue_auto_inventory_reorder(viewer, verbose_status: bool = True) -> int:\n    if viewer.auto_inventory_reorder_job_running:\n        if verbose_status:\n            viewer.set_status("Inventory Sort: already running")\n        return 0\n    if viewer.auto_outpost_store_job_running or viewer.auto_id_job_running or viewer.auto_salvage_job_running or viewer.auto_buy_kits_job_running or viewer.auto_gold_balance_job_running:\n        if verbose_status:\n            viewer.set_status("Inventory Sort: busy with another inventory job")\n        return 0\n    try:\n        GLOBAL_CACHE.Coroutines.append(viewer._run_auto_inventory_reorder_job(verbose_status=verbose_status))\n        if verbose_status:\n            viewer.set_status("Inventory Sort: started")\n        return 1\n    except EXPECTED_RUNTIME_ERRORS as e:\n        if verbose_status:\n            viewer.set_status(f"Inventory Sort queue failed: {e}")\n        return 0',
    'run_auto_inventory_reorder_once_on_outpost_entry_tick': 'def _outpost_impl_run_auto_inventory_reorder_once_on_outpost_entry_tick(viewer) -> bool:\n    entry_key = viewer._refresh_auto_outpost_store_entry_key()\n    if not entry_key:\n        return False\n    if not viewer.auto_buy_kits_sort_to_front_enabled:\n        return False\n    if viewer.auto_inventory_reorder_handled_entry_key == entry_key:\n        return False\n    if viewer.auto_outpost_store_job_running or viewer.auto_id_job_running or viewer.auto_salvage_job_running or viewer.auto_buy_kits_job_running or viewer.auto_gold_balance_job_running or viewer.auto_inventory_reorder_job_running:\n        return False\n\n    queued = viewer._queue_auto_inventory_reorder(verbose_status=False)\n    if queued > 0:\n        viewer.auto_inventory_reorder_handled_entry_key = entry_key\n        return True\n    return False',
    'run_auto_buy_kits_once_on_outpost_entry_tick': 'def _outpost_impl_run_auto_buy_kits_once_on_outpost_entry_tick(viewer) -> bool:\n    entry_key = viewer._refresh_auto_outpost_store_entry_key()\n    if not entry_key:\n        viewer._trace_auto_buy_kits("entry: skipped, no entry key")\n        return False\n    if not viewer.auto_buy_kits_enabled:\n        viewer._trace_auto_buy_kits("entry: skipped, feature disabled")\n        return False\n    if not viewer._is_auto_buy_kits_allowed_outpost():\n        viewer._trace_auto_buy_kits("entry: skipped, outpost not in allowlist")\n        return False\n    if viewer.auto_buy_kits_handled_entry_key == entry_key:\n        viewer._trace_auto_buy_kits(f"entry: skipped, already handled entry={entry_key}")\n        return False\n    if viewer.auto_outpost_store_job_running or viewer.auto_id_job_running or viewer.auto_salvage_job_running or viewer.auto_buy_kits_job_running or viewer.auto_gold_balance_job_running or viewer.auto_inventory_reorder_job_running:\n        viewer._trace_auto_buy_kits("entry: skipped, busy with other running job")\n        return False\n\n    stats = viewer._collect_local_inventory_kit_stats()\n    salvage_uses = max(0, viewer._safe_int(stats.get("salvage_uses", 0), 0))\n    total_id_uses = max(0, viewer._safe_int(viewer._collect_total_identification_uses(), 0))\n    if salvage_uses >= 25 and total_id_uses >= 25:\n        viewer._trace_auto_buy_kits(\n            f"entry: skipped, enough kits (salvage_uses={salvage_uses}, total_id_uses={total_id_uses})"\n        )\n        viewer.auto_buy_kits_handled_entry_key = entry_key\n        return False\n\n    queued = viewer._queue_buy_kits_if_needed(verbose_status=True)\n    if queued > 0:\n        viewer._trace_auto_buy_kits(f"entry: queued job for entry={entry_key}")\n        viewer.auto_buy_kits_handled_entry_key = entry_key\n        return True\n    viewer._trace_auto_buy_kits(f"entry: queue failed for entry={entry_key}")\n    return False',
    'refresh_auto_outpost_store_entry_key': 'def _outpost_impl_refresh_auto_outpost_store_entry_key(viewer) -> str:\n    is_outpost = False\n    map_id = 0\n    instance_uptime_ms = 0\n    try:\n        is_outpost = bool(Map.IsOutpost())\n        map_id = max(0, viewer._safe_int(Map.GetMapID(), 0))\n        instance_uptime_ms = max(0, viewer._safe_int(Map.GetInstanceUptime(), 0))\n    except EXPECTED_RUNTIME_ERRORS:\n        is_outpost = False\n\n    if not is_outpost:\n        viewer.auto_outpost_store_last_is_outpost = False\n        viewer.auto_outpost_store_last_map_id = 0\n        viewer.auto_outpost_store_last_instance_uptime_ms = 0\n        viewer.auto_outpost_store_current_entry_key = ""\n        return ""\n\n    entered_new_outpost = False\n    if not viewer.auto_outpost_store_last_is_outpost:\n        entered_new_outpost = True\n    elif map_id != viewer.auto_outpost_store_last_map_id:\n        entered_new_outpost = True\n    elif (\n        viewer.auto_outpost_store_last_instance_uptime_ms > 0\n        and instance_uptime_ms > 0\n        and instance_uptime_ms + 2000 < viewer.auto_outpost_store_last_instance_uptime_ms\n    ):\n        entered_new_outpost = True\n\n    if entered_new_outpost or not viewer.auto_outpost_store_current_entry_key:\n        viewer.auto_outpost_store_entry_nonce += 1\n        viewer.auto_outpost_store_current_entry_key = f"{map_id}:{viewer.auto_outpost_store_entry_nonce}"\n\n    viewer.auto_outpost_store_last_is_outpost = True\n    viewer.auto_outpost_store_last_map_id = map_id\n    viewer.auto_outpost_store_last_instance_uptime_ms = instance_uptime_ms\n    return viewer.auto_outpost_store_current_entry_key',
    'deposit_materials_and_tomes_to_storage': 'def _outpost_impl_deposit_materials_and_tomes_to_storage(viewer):\n    deposit_count = 0\n    try:\n        bags_to_check = ItemArray.CreateBagList(1, 2, 3, 4)\n        item_array = list(ItemArray.GetItemArray(bags_to_check) or [])\n    except EXPECTED_RUNTIME_ERRORS:\n        item_array = []\n\n    for raw_item_id in item_array:\n        item_id = max(0, viewer._safe_int(raw_item_id, 0))\n        if item_id <= 0:\n            continue\n\n        try:\n            is_material = bool(GLOBAL_CACHE.Item.Type.IsMaterial(item_id))\n        except EXPECTED_RUNTIME_ERRORS:\n            is_material = False\n\n        try:\n            is_tome = bool(GLOBAL_CACHE.Item.Type.IsTome(item_id))\n        except EXPECTED_RUNTIME_ERRORS:\n            is_tome = False\n\n        if not is_material and not is_tome:\n            continue\n\n        try:\n            GLOBAL_CACHE.Inventory.DepositItemToStorage(item_id)\n        except EXPECTED_RUNTIME_ERRORS:\n            continue\n\n        deposit_count += 1\n        try:\n            yield from Routines.Yield.wait(350)\n        except EXPECTED_RUNTIME_ERRORS:\n            pass\n\n    return int(deposit_count)',
    'sell_gold_items_except_runes': 'def _outpost_impl_sell_gold_items_except_runes(viewer):\n    sell_item_ids: list[int] = []\n    try:\n        bags_to_check = ItemArray.CreateBagList(1, 2, 3, 4)\n        item_array = list(ItemArray.GetItemArray(bags_to_check) or [])\n    except EXPECTED_RUNTIME_ERRORS:\n        item_array = []\n\n    for raw_item_id in item_array:\n        item_id = max(0, viewer._safe_int(raw_item_id, 0))\n        if item_id <= 0:\n            continue\n\n        try:\n            rarity_name = viewer._ensure_text(Item.Rarity.GetRarity(item_id)[1]).strip() or "Unknown"\n        except EXPECTED_RUNTIME_ERRORS:\n            rarity_name = "Unknown"\n        if rarity_name != "Gold":\n            continue\n\n        try:\n            item_name = viewer._ensure_text(Item.GetName(item_id)).strip()\n        except EXPECTED_RUNTIME_ERRORS:\n            item_name = ""\n        if "rune" in item_name.lower():\n            continue\n\n        sell_item_ids.append(item_id)\n\n    if not sell_item_ids:\n        return int(0)\n\n    merchant_open = False\n    try:\n        merchant_open = bool(viewer._is_merchant_frame_open())\n    except EXPECTED_RUNTIME_ERRORS:\n        merchant_open = False\n\n    if not merchant_open:\n        try:\n            merchant_candidates = list(viewer._get_nearby_merchant_candidate_agent_ids() or [])\n        except EXPECTED_RUNTIME_ERRORS:\n            merchant_candidates = []\n        for candidate_id in merchant_candidates[:12]:\n            cid = max(0, viewer._safe_int(candidate_id, 0))\n            if cid <= 0:\n                continue\n            try:\n                merchant_open = bool((yield from viewer._approach_and_open_merchant(cid)))\n            except EXPECTED_RUNTIME_ERRORS:\n                merchant_open = False\n            if merchant_open:\n                break\n\n    if not merchant_open:\n        return int(0)\n\n    try:\n        yield from Routines.Yield.wait(250)\n    except EXPECTED_RUNTIME_ERRORS:\n        pass\n\n    try:\n        yield from Routines.Yield.Merchant.SellItems(sell_item_ids, log=False)\n    except EXPECTED_RUNTIME_ERRORS:\n        return int(0)\n\n    return int(len(sell_item_ids))',
    'run_manual_sell_gold_items_job': 'def _outpost_impl_run_manual_sell_gold_items_job(viewer):\n    viewer.auto_outpost_store_job_running = True\n    sold = 0\n    try:\n        sold = int((yield from viewer._sell_gold_items_except_runes()) or 0)\n        if sold > 0:\n            viewer.set_status(f"Sell Gold: sold {sold} gold items (runes kept)")\n        else:\n            viewer.set_status("Sell Gold: no sellable gold items found")\n    except EXPECTED_RUNTIME_ERRORS as e:\n        viewer.set_status(f"Sell Gold failed: {e}")\n    finally:\n        viewer.auto_outpost_store_job_running = False',
    'queue_manual_sell_gold_items': 'def _outpost_impl_queue_manual_sell_gold_items(viewer) -> int:\n    if viewer.auto_outpost_store_job_running or viewer.auto_id_job_running or viewer.auto_salvage_job_running or viewer.auto_buy_kits_job_running or viewer.auto_gold_balance_job_running or viewer.auto_inventory_reorder_job_running:\n        viewer.set_status("Sell Gold: busy with another inventory job")\n        return 0\n    try:\n        GLOBAL_CACHE.Coroutines.append(viewer._run_manual_sell_gold_items_job())\n        viewer.set_status("Sell Gold: started")\n        return 1\n    except EXPECTED_RUNTIME_ERRORS as e:\n        viewer.set_status(f"Sell Gold queue failed: {e}")\n        return 0',
    'run_outpost_store_job': 'def _outpost_impl_run_outpost_store_job(viewer, entry_key: str):\n    viewer.auto_outpost_store_job_running = True\n    deposited = 0\n    try:\n        deposited = int((yield from viewer._deposit_materials_and_tomes_to_storage()) or 0)\n        if deposited > 0:\n            viewer.set_status(f"Outpost Store: deposited {deposited} materials/tomes")\n        else:\n            viewer.set_status("Outpost Store: no materials/tomes found")\n    except EXPECTED_RUNTIME_ERRORS as e:\n        viewer.set_status(f"Outpost Store failed: {e}")\n    finally:\n        viewer.auto_outpost_store_job_running = False\n        if entry_key:\n            viewer.auto_outpost_store_handled_entry_key = entry_key',
    'run_auto_outpost_store_tick': 'def _outpost_impl_run_auto_outpost_store_tick(viewer, force: bool = False) -> bool:\n    entry_key = viewer._refresh_auto_outpost_store_entry_key()\n    if not entry_key:\n        return False\n    if not force and not viewer.auto_outpost_store_enabled:\n        return False\n    if viewer.auto_outpost_store_job_running or viewer.auto_id_job_running or viewer.auto_salvage_job_running or viewer.auto_buy_kits_job_running or viewer.auto_gold_balance_job_running or viewer.auto_inventory_reorder_job_running:\n        return False\n    if viewer.auto_outpost_store_handled_entry_key == entry_key:\n        return False\n    try:\n        GLOBAL_CACHE.Coroutines.append(viewer._run_outpost_store_job(entry_key))\n        viewer.auto_outpost_store_handled_entry_key = entry_key\n        viewer.set_status("Outpost Store: started (materials+tomes)")\n        return True\n    except EXPECTED_RUNTIME_ERRORS as e:\n        viewer.set_status(f"Outpost Store queue failed: {e}")\n        return False',
}

def get_nearby_merchant_candidate_agent_ids(viewer, *args, **kwargs):
    return _bind_impl(viewer, 'get_nearby_merchant_candidate_agent_ids')(viewer, *args, **kwargs)

def find_nearby_merchant_agent_id(viewer, *args, **kwargs):
    return _bind_impl(viewer, 'find_nearby_merchant_agent_id')(viewer, *args, **kwargs)

def is_merchant_frame_open(viewer, *args, **kwargs):
    return _bind_impl(viewer, 'is_merchant_frame_open')(viewer, *args, **kwargs)

def handle_lions_arch_merchant_dialog_if_visible(viewer, *args, **kwargs):
    return _bind_impl(viewer, 'handle_lions_arch_merchant_dialog_if_visible')(viewer, *args, **kwargs)

def get_offered_merchant_items(viewer, *args, **kwargs):
    return _bind_impl(viewer, 'get_offered_merchant_items')(viewer, *args, **kwargs)

def find_merchant_item_by_model(viewer, *args, **kwargs):
    return _bind_impl(viewer, 'find_merchant_item_by_model')(viewer, *args, **kwargs)

def build_auto_buy_kits_merchant_debug_report(viewer, *args, **kwargs):
    return _bind_impl(viewer, 'build_auto_buy_kits_merchant_debug_report')(viewer, *args, **kwargs)

def dump_auto_buy_kits_merchant_debug_report(viewer, *args, **kwargs):
    return _bind_impl(viewer, 'dump_auto_buy_kits_merchant_debug_report')(viewer, *args, **kwargs)

def build_inventory_slot_order(viewer, *args, **kwargs):
    return _bind_impl(viewer, 'build_inventory_slot_order')(viewer, *args, **kwargs)

def collect_kit_item_ids_for_sort(viewer, *args, **kwargs):
    return _bind_impl(viewer, 'collect_kit_item_ids_for_sort')(viewer, *args, **kwargs)

def collect_kit_quantity_map(viewer, *args, **kwargs):
    return _bind_impl(viewer, 'collect_kit_quantity_map')(viewer, *args, **kwargs)

def is_rune_item_for_reorder(viewer, *args, **kwargs):
    return _bind_impl(viewer, 'is_rune_item_for_reorder')(viewer, *args, **kwargs)

def inventory_reorder_bucket(viewer, *args, **kwargs):
    return _bind_impl(viewer, 'inventory_reorder_bucket')(viewer, *args, **kwargs)

def reorder_inventory_after_kits(viewer, *args, **kwargs):
    return _bind_impl(viewer, 'reorder_inventory_after_kits')(viewer, *args, **kwargs)

def sort_kits_to_front(viewer, *args, **kwargs):
    return _bind_impl(viewer, 'sort_kits_to_front')(viewer, *args, **kwargs)

def buy_item_from_merchant(viewer, *args, **kwargs):
    return _bind_impl(viewer, 'buy_item_from_merchant')(viewer, *args, **kwargs)

def approach_and_open_merchant(viewer, *args, **kwargs):
    return _bind_impl(viewer, 'approach_and_open_merchant')(viewer, *args, **kwargs)

def run_buy_kits_if_needed_job(viewer, *args, **kwargs):
    return _bind_impl(viewer, 'run_buy_kits_if_needed_job')(viewer, *args, **kwargs)

def queue_buy_kits_if_needed(viewer, *args, **kwargs):
    return _bind_impl(viewer, 'queue_buy_kits_if_needed')(viewer, *args, **kwargs)

def run_auto_gold_balance_job(viewer, *args, **kwargs):
    return _bind_impl(viewer, 'run_auto_gold_balance_job')(viewer, *args, **kwargs)

def queue_auto_gold_balance(viewer, *args, **kwargs):
    return _bind_impl(viewer, 'queue_auto_gold_balance')(viewer, *args, **kwargs)

def run_auto_gold_balance_once_on_outpost_entry_tick(viewer, *args, **kwargs):
    return _bind_impl(viewer, 'run_auto_gold_balance_once_on_outpost_entry_tick')(viewer, *args, **kwargs)

def run_auto_inventory_reorder_job(viewer, *args, **kwargs):
    return _bind_impl(viewer, 'run_auto_inventory_reorder_job')(viewer, *args, **kwargs)

def queue_auto_inventory_reorder(viewer, *args, **kwargs):
    return _bind_impl(viewer, 'queue_auto_inventory_reorder')(viewer, *args, **kwargs)

def run_auto_inventory_reorder_once_on_outpost_entry_tick(viewer, *args, **kwargs):
    return _bind_impl(viewer, 'run_auto_inventory_reorder_once_on_outpost_entry_tick')(viewer, *args, **kwargs)

def run_auto_buy_kits_once_on_outpost_entry_tick(viewer, *args, **kwargs):
    return _bind_impl(viewer, 'run_auto_buy_kits_once_on_outpost_entry_tick')(viewer, *args, **kwargs)

def refresh_auto_outpost_store_entry_key(viewer, *args, **kwargs):
    return _bind_impl(viewer, 'refresh_auto_outpost_store_entry_key')(viewer, *args, **kwargs)

def deposit_materials_and_tomes_to_storage(viewer, *args, **kwargs):
    return _bind_impl(viewer, 'deposit_materials_and_tomes_to_storage')(viewer, *args, **kwargs)

def sell_gold_items_except_runes(viewer, *args, **kwargs):
    return _bind_impl(viewer, 'sell_gold_items_except_runes')(viewer, *args, **kwargs)

def run_manual_sell_gold_items_job(viewer, *args, **kwargs):
    return _bind_impl(viewer, 'run_manual_sell_gold_items_job')(viewer, *args, **kwargs)

def queue_manual_sell_gold_items(viewer, *args, **kwargs):
    return _bind_impl(viewer, 'queue_manual_sell_gold_items')(viewer, *args, **kwargs)

def run_outpost_store_job(viewer, *args, **kwargs):
    return _bind_impl(viewer, 'run_outpost_store_job')(viewer, *args, **kwargs)

def run_auto_outpost_store_tick(viewer, *args, **kwargs):
    return _bind_impl(viewer, 'run_auto_outpost_store_tick')(viewer, *args, **kwargs)
